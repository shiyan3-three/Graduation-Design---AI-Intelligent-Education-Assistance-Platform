# Review 2026-04-15

## B3-2 扩展复审（命令行）

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `knowledge_tool` 当前在 `snippet` 缺失时会回退到整段 `content`，真实结果长度达到 3-4 KB，首版可用但后续建议增加截断或清洗，避免回复过长
- [ ] 本地 `unittest` 在沙箱内会因临时目录或 SQLite 写入受限失败，需要提权重跑；属于验证环境限制，不是业务阻塞

## 是否满足 acceptance
- [x] 是
- [x] `backend/tools/knowledge_tool.py` 存在，函数签名符合 `knowledge_tool(topic: str) -> str`
- [x] 代码内部调用百度千帆 `POST /v2/ai_search/web_search`，未使用 Wikipedia API
- [x] `BAIDU_SEARCH_API_KEY` 通过环境变量读取，未硬编码
- [x] 单测覆盖 API Key 缺失与请求失败，工具返回错误文本、不抛出异常
- [x] 真实 HTTP 验证：`什么是勾股定理`、`请解释牛顿第一定律` 均触发 `knowledge_tool`，助手回复包含搜索摘要
- [x] 真实 HTTP 验证：`tools_used[0].tool_name == "knowledge_tool"`
- [x] 回归验证：`计算 2 的 10 次方`、`请计算 1+1`、`请计算 sqrt(16)`、`请计算 sin(pi/2)` 均由 `calculator_tool` 正确处理；`请计算 2 + ) 的结果` 返回错误文本且服务未崩
- [x] 后端服务已在 `127.0.0.1:8000` 正常监听，完整 `unittest` 提权后 15/15 通过

## open-issues 补充
- 当前真实搜索结果未做站点过滤，返回来源会混入百科、资讯站和内容平台；本轮不构成阻塞，但后续如进入正式阶段建议补充来源约束或摘要清洗

## 建议下一步
1. Leader Agent 可收口 `B3-2 knowledge_tool`
2. 进入 `F2-3` 前端最小工具调用状态展示
3. 如要提升可控性，可单独立项做知识来源过滤与摘要长度控制

## Human Verification Note
- Correction: the latest coding progress is the current task `F2-3`, not `B3-2`.
- Human feedback for the latest coding progress: this round of manual testing found no obvious bugs.
- Review status: record this as manual evidence for the upcoming `F2-3` review; it does not by itself close the task.

## F2-3 Review

## Conclusion
- [x] Pass

## Blockers
- [ ] None

## Non-blocking
- [ ] No new non-blocking issues were identified in this round.

## Acceptance
- [x] `chat-state.js` keeps `message.tools_used` and exposes `message.toolsUsed`
- [x] `chat-view.js` includes `toolsUsed` in the view-model mapping and maps known tool names to Chinese labels
- [x] Assistant replies with non-empty `tools_used` render visible tool tags in the bubble
- [x] Success and error tool calls render with distinct classes and text
- [x] Replies without tool usage do not render a tool-tag area
- [x] Historical session messages still render tool tags after loading `/api/sessions/{id}`
- [x] Both `app.js` and `sidebar-app.js` render messages through `createMessageElement`, now shared by `message-element.js`
- [x] `styles.css` includes `.tool-tags`, `.tool-tag`, `.tool-tag.is-success`, and `.tool-tag.is-error`
- [x] Frontend service started on `127.0.0.1:5173`, and `/`, `/src/app.js`, `/styles.css` all returned `200`
- [x] No regression found: `npm test` passed 21/21, frontend proxy `GET /api/sessions` returned `success: true`, and human manual testing reported no obvious bugs

## Open Issues
- None added in this review round.

## Next
1. Leader Agent can close `F2-3`
2. Move to `B3-3`
