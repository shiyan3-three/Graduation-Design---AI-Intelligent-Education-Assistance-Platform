# 2026-04-17

## 当日关键进展

- 完成 B3-3 `question_tool`：两级降级（本地 C-Eval 题库 + LLM 兜底出题），从 HuggingFace 下载 7 学科 CSV 共 1396 题导入 `question_bank` 表。
- 修复 Review 指出的 `question_tool.py` 和 `import_ceval.py` SQLite 连接泄漏（ResourceWarning），改为显式 `close()`。
- 完成 M4-1 最小 MCP Server 骨架：`backend/mcp_server.py` 使用 `FastMCP("EduTools")` 注册 `calculator_tool`；`backend/services/mcp_client.py` 通过 stdio client 调用。
- `llm.py` 中 `calculator_tool` 从直接函数调用切换为 MCP 优先 + direct fallback。
- 修复 M4-1 Review 问题：`mcp_server.py` 脚本模式下 `sys.path` 补项目根目录，解决 `ModuleNotFoundError`。
- 更新 `knowledge_tool` 切换到百度千帆新接口（`choices + references` 返回结构）。

## 关键决策

- `question_tool` 三级降级简化为两级（本地题库 + LLM 兜底），跳过联网搜索出题 → 联网搜索格式不可控、答辩风险大。
- C-Eval 数据集改用 `httpx + pandas + pyarrow` 下载，绕过 HuggingFace `datasets` 库兼容问题。
- MCP Client 暂用"每次新建 stdio 连接"的最小实现，连接复用优化暂不立项。
- M4-1 只迁移 `calculator_tool` 到 MCP，`knowledge_tool` 和 `question_tool` 留给 M4-2。
- 验证使用 `project-scipt/runtime/backend_runtime.db` 避免沙箱只读问题。

## 当前遗留问题

- MCP Client 每次调用新建 stdio 子进程，性能不佳，保留在 open-issues 中。
- MCP Python SDK 版本迭代快，AI 生成的 MCP 代码可能存在 API 幻觉，需参考最新文档。
- 前端 `question_tool` 标签映射待完善（不影响渲染但 Review 关注）。

## 给下一个 Agent 的交接

- B3-3 和 M4-1 均已完成并通过 Review，下一步应做 M4-2（全部工具 MCP 封装 + 调用路径切换）。
- 后端 31 项测试全部通过，前端测试也全部通过。
- `backend/.env` 中现有 `BAIDU_SEARCH_API_KEY`，C-Eval 题库数据在 `backend/tools/ceval_data/`。
- MCP server 可通过 `python -m backend.mcp_server` 或 `python backend/mcp_server.py` 启动。
