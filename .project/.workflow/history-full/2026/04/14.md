# 2026-04-14

## 任务：F2-1 前端最简对话页发送与展示

### 做了什么
- 按 `.project/.workflow/current/`、`.project/.workflow/project/`、最近 `history-ai` 和最新 `review` 读取协作上下文，确认本轮只做前端最简对话页，不扩展会话侧栏、登录、流式输出或后端功能
- 读取 `.project/api-design.md` 第 5.3 节与 `backend/routers/chat.py`，确认前端请求体必须为 `{ content, session_id }`，并从响应里复用 `data.session.id`
- 结合当前仓库没有 `frontend/` 目录、后端未配置 CORS、依赖安装受限等实际情况，放弃引入 Vite/React，改为实现“原生 HTML + ES Module + Node 内置静态服务 + 同源 `/api/chat` 代理”的最小骨架
- 先新增 `frontend/tests/chat-state.test.js`，以 TDD 方式覆盖请求载荷、会话 ID 复用、成功/失败状态更新和视图模型
- 新增 `frontend/src/chat-state.js`、`frontend/src/chat-view.js`、`frontend/src/app.js`，把状态流转、视图映射和 DOM 交互拆开，保证页面可以渲染消息、显示加载态并在失败时提示错误
- 新增 `frontend/index.html`、`frontend/styles.css`、`frontend/server.js` 和 `frontend/package.json`，完成可直接 `npm run dev` 启动的最简前端服务
- 运行 `npm test`，确认前端状态逻辑与视图模型测试全部通过
- 由于本地 `.venv` 缺少 `uvicorn`/`fastapi`，先安装 `backend/requirements.txt` 后再继续联调
- 联调时发现 `backend/app.db` 在当前环境下为只读，无法直接启动后端；验证阶段改为在提权环境下使用独立验证库启动后端，以避免修改后端代码
- 通过真实 HTTP 请求验证页面可访问、页面骨架存在输入框/发送按钮、前端代理能调用 `/api/chat`、第二次请求可复用首次返回的 `session_id`
- 额外用 Node 内联 DOM stub 执行 `frontend/src/app.js`，验证初始渲染、成功发送和失败提示三条前端 UI 主链路逻辑

### 改动文件
- `frontend/package.json` - 新增前端脚本入口，提供 `npm run dev` 和 `npm test`
- `frontend/tests/chat-state.test.js` - 新增前端状态与视图模型测试
- `frontend/src/chat-state.js` - 新增提交前/成功后/失败后的纯函数状态流转
- `frontend/src/chat-view.js` - 新增页面展示所需的视图模型映射
- `frontend/src/app.js` - 新增页面 DOM 绑定、消息渲染、发送逻辑与错误处理
- `frontend/server.js` - 新增 Node 内置静态服务与 `/api/chat` 同源代理
- `frontend/index.html` - 新增最简对话页骨架
- `frontend/styles.css` - 新增最简页面样式，区分用户/AI 消息并提供基础布局
- `.project/.workflow/history-full/2026/04/14.md` - 记录本轮完整执行日志

### 关键决策
- 本轮不用 Vite/React，改用原生 HTML + JS：任务目标只要求跑通最短前端链路，当前仓库无前端基础设施，且直接引入依赖会把重点转移到安装和构建问题上
- 使用 `frontend/server.js` 做同源 `/api/chat` 代理：后端当前未见 CORS 配置，代理可以在不改后端代码的前提下保证浏览器主链路可用
- 把前端逻辑拆为 `chat-state` + `chat-view` + `app`：前两者保持纯函数，便于 TDD 和无浏览器环境下的自验证，`app.js` 只负责 DOM 与网络胶水
- 验证时安装后端依赖并切换验证数据库，而不改后端实现：遵守当前任务“不改后端代码”的边界，同时尽量完成自主联调

### 遇到的问题
- 用户给出的工作流根路径是 `.workflow/`，但项目实际路径在 `.project/.workflow/`；本轮按 `project/decisions.md` 约定执行
- `node --test` 在当前环境触发 `spawn EPERM`，无法正常跑测试；改为直接执行测试文件 `node tests/chat-state.test.js`
- 本地 `.venv` 初始缺少 `uvicorn`、`fastapi` 等依赖，导致后端无法启动；补装 `backend/requirements.txt` 后继续
- 后端启动时写 `backend/app.db` 报 `attempt to write a readonly database`；后续验证改用独立验证库并在提权环境下启动后端
- 在当前沙箱内直接新建/写入 SQLite 验证库会触发 `disk I/O error`，因此最终验证采用沙箱外后端启动方式完成

### 当前结果
- `frontend/` 目录已具备可运行的最简前端服务，`npm run dev` 可启动页面服务
- 页面骨架已包含消息输入框和发送按钮，用户消息与 AI 回复在 UI 上可区分
- 前端通过同源代理成功调用 `POST /api/chat`，并可复用返回的 `session_id` 继续会话
- 页面逻辑具备发送中按钮禁用/文案变化，以及请求失败后的错误提示
- `npm test` 已通过；真实 HTTP 联调和页面脚本冒烟验证也已完成，可进入人类验证 / Review Agent 阶段

## 任务：Windows 一键启动/关闭脚本

### 做了什么
- 在 `project-scipt/` 下新增 4 个可双击使用的 Windows `.bat` 脚本，分别用于启动/关闭前端和后端
- 为启动脚本补充端口占用检查，避免重复启动
- 为前端启动脚本补充 Node.js 检测，并将日志输出到 `project-scipt/logs/frontend.log`
- 为后端启动脚本补充 `.venv` Python 检测，并默认把运行数据库切到 `project-scipt/runtime/backend_runtime.db`，避开本地只读 `backend/app.db`
- 为两个关闭脚本补充按端口查 PID 再结束进程的逻辑，并增加 `NO_PAUSE=1` 开关，兼容双击使用和命令行自动验证
- 在提权环境下完整验证四个脚本：前端可启动并监听 `5173`，后端可启动并监听 `8000`，两个关闭脚本也都能把对应服务停掉

### 改动文件
- `project-scipt/start-frontend.bat` - 新增前端一键启动脚本
- `project-scipt/stop-frontend.bat` - 新增前端一键关闭脚本
- `project-scipt/start-backend.bat` - 新增后端一键启动脚本
- `project-scipt/stop-backend.bat` - 新增后端一键关闭脚本
- `.project/.workflow/history-full/2026/04/14.md` - 追加本轮脚本工作记录

### 关键决策
- 脚本全部使用 `.bat`：符合“Windows 双击即用”的要求
- 关闭脚本按固定端口 `5173` / `8000` 查找并结束进程：避免依赖额外 PID 文件
- 启动脚本默认写日志到 `project-scipt/logs/`：启动失败时可直接查看原因
- 后端脚本默认使用 `project-scipt/runtime/backend_runtime.db`：减少本机环境里只读 `app.db` 带来的启动失败概率

### 遇到的问题
- `.bat` 在 `cmd.exe` 下对 UTF-8 + LF + 中文输出兼容性较差，初版脚本出现了解析异常；后续改为 ASCII 提示和单行启动命令
- 关闭脚本初版的子程序标签解析异常，已改为内联结束进程逻辑
- 当前桌面线程里执行 stop 脚本时会额外看到一条 `Input redirection is not supported`，但不影响脚本实际停掉服务

### 当前结果
- 双击 `project-scipt/start-frontend.bat` 可启动前端，地址为 `http://127.0.0.1:5173`
- 双击 `project-scipt/start-backend.bat` 可启动后端，地址为 `http://127.0.0.1:8000`
- 双击两个 stop 脚本可关闭对应服务
- 当前我已经把验证过程中启动的前后端都关掉，端口处于干净状态

## 任务：F2-2 前后端 chat 联调与字段对齐

### 做了什么
- 重新读取 `.project/.workflow/current/`、`.project/.workflow/project/`、最新 `review/2026/04/14.md`、`roadmap.md` 与《系统模块设计方案》，确认 Leader 将当前任务切到 F2-2 是合理的：它对应第 2 阶段“前后端 chat 联调”和“字段保持一致”的目标
- 使用 `project-scipt/start-frontend.bat` 与 `project-scipt/start-backend.bat` 启动前后端，并确认 `http://127.0.0.1:5173/` 与 `http://127.0.0.1:8000/api/sessions` 均返回 `200`
- 通过命令行连续向 `http://127.0.0.1:5173/api/chat` 发送 3 轮请求，记录每轮请求体与响应关键字段，验证 `session_id` 串联行为
- 额外执行 `frontend/src/app.js` 的页面脚本级验证：用 DOM stub 连续触发 3 次提交，直接记录前端实际发出的 3 个请求体，确认 `currentSessionId` 从 `data.session.id` 获取后被第 2/3 轮复用
- 验证完成后使用 `project-scipt/stop-frontend.bat` 与 `project-scipt/stop-backend.bat` 清理运行中的服务

### 改动文件
- `.project/.workflow/history-full/2026/04/14.md` - 追加 F2-2 联调验证过程与结果

### 关键决策
- 虽然任务描述偏向“真实浏览器 + DevTools”，但 `acceptance.md` 明确允许“命令行代理日志展示至少 2 轮请求体和响应体”，因此本轮以命令行证据 + 页面脚本级验证完成 F2-2
- 在未发现字段不对齐前，不主动改前端代码，避免把验证任务扩大为重构任务

### 遇到的问题
- 当前桌面线程没有可直接控制的浏览器 DevTools，因此未生成真实 Network 截图；本轮用命令行请求/响应记录和页面脚本执行结果替代，仍满足 acceptance 的备选证据要求

### 当前结果
- 前后端可同时启动，`5173` 和 `8000` 端口可访问
- 3 轮连续请求中，第 1 轮请求体为 `{ content: 'round-1 explain derivative', session_id: null }`
- 第 2、3 轮请求体中的 `session_id` 都为第 1 轮响应返回的 `data.session.id = 6`
- 三轮响应均包含 `data.session.id` 与 `data.assistant_message.content`，字段路径与 `api-design.md` 第 5.3 节一致
- 页面脚本级验证中，前端实际发出的 3 个请求体依次为 `null → 42 → 42`，说明 `currentSessionId` 维护逻辑正确
- 本轮未发现需要修正的字段名或路径问题，因此无业务代码改动

## 任务：F2-侧栏 对话历史侧栏的最小占位或基础列表

### 做了什么
- 重新读取 `.project/.workflow/current/`、`.project/.workflow/project/roadmap.md`、`.project/系统模块设计方案.md` 与最新 review，确认 Leader 将当前任务切到 F2 侧栏是合理的，且应以 roadmap 与系统模块设计方案为准执行
- 核对 `backend/routers/sessions.py`、`backend/models.py` 与 `backend/tests/test_sessions_api.py`，确认真实接口为 `GET /api/sessions -> data.items`、`GET /api/sessions/{id} -> data.session + data.messages`，并据此修正实现假设
- 新增 `frontend/tests/sidebar-state.test.js`，以 TDD 方式先覆盖侧栏加载、会话选中、会话详情替换和视图模型暴露
- 扩展 `frontend/src/chat-state.js` 与 `frontend/src/chat-view.js`，加入 `sessions`、`isLoadingSessions`、`isLoadingSessionDetail` 及对应纯函数状态流转
- 新增 `frontend/chat-sidebar.html`、`frontend/chat-sidebar.css`、`frontend/src/sidebar-app.js` 承载本轮侧栏页面与脚本，并把 `frontend/server.js` 的静态入口切到新文件
- 在 `frontend/server.js` 中补上 `/api/sessions` 与 `/api/sessions/:id` 的同源代理，保持前端继续通过 `5173` 访问全部接口
- 运行 `npm test`，确认旧有对话链路测试和新增侧栏状态测试全部通过
- 启动前后端后，分别验证 `http://127.0.0.1:5173/`、`http://127.0.0.1:5173/api/sessions`、`http://127.0.0.1:5173/api/sessions/1` 与后端原始 `http://127.0.0.1:8000/api/sessions/1`，确认页面入口与代理链路可用
- 使用 Node 轻量 DOM stub 导入 `frontend/src/sidebar-app.js` 做页面脚本级联调：首屏拿到 7 条历史会话，点击最旧会话 `#1` 后主区加载出 8 条消息，再发送 `session-refresh-check` 后该会话被刷新到侧栏顶部且主区消息数增至 10

### 改动文件
- `frontend/package.json` - 调整测试脚本，兼容当前 Windows 环境下 `node --test` 的 `spawn EPERM`
- `frontend/tests/sidebar-state.test.js` - 新增侧栏状态与视图模型测试
- `frontend/src/chat-state.js` - 增加历史会话列表和会话详情加载相关状态与纯函数
- `frontend/src/chat-view.js` - 暴露侧栏列表、加载提示和空态所需的视图模型
- `frontend/server.js` - 切换新的静态入口页，并新增 `/api/sessions` 与 `/api/sessions/:id` 代理
- `frontend/chat-sidebar.html` - 新增包含历史会话侧栏的页面骨架
- `frontend/chat-sidebar.css` - 新增侧栏与主对话区联动样式
- `frontend/src/sidebar-app.js` - 新增页面入口脚本，负责首屏拉历史会话、切换会话详情、发送后刷新侧栏
- `.project/.workflow/history-full/2026/04/14.md` - 追加本轮工作日志

### 关键决策
- 以真实后端实现和测试为准，而不是直接照搬 `context.md` 中过时的 `data.sessions` 描述，避免把联调问题引入到前端
- 保持 F2-1 的最小实现文件不做大规模重写，改为新增侧栏入口页并由 `server.js` 切换静态映射，降低回归风险
- 发送成功后的侧栏更新采用“重新请求 `/api/sessions` 刷新列表”的方式，而不是在前端本地猜测排序与标题，保证与后端 `updated_at DESC` 行为一致

### 遇到的问题
- 当前环境中 `node --test` 会触发 `spawn EPERM`，因此测试脚本改为顺序执行 `node tests/chat-state.test.js && node tests/sidebar-state.test.js`
- `.project/.workflow/history-full/2026/04/14.md` 在终端输出时存在历史编码混杂现象，因此本轮只追加新记录，不改写旧内容
- `context.md` 中的 `/api/sessions` 响应字段与真实后端不一致，已通过阅读后端代码与接口实测确认应使用 `data.items`

### 当前结果
- 页面已具备左侧历史会话栏，首屏会请求 `GET /api/sessions` 并展示会话标题列表
- 点击历史会话会请求 `GET /api/sessions/{id}`，主区消息可切换到目标会话
- 在已选会话下继续发送消息后，侧栏列表会重新刷新，且被更新的会话会移动到顶部
- `npm test` 已通过，脚本级联调也已通过；当前前后端服务处于运行中，便于人类继续验证或直接进入 Review

## 任务：B3-1 后端接入 `calculator_tool`（预备阶段）

### 做了什么
- 重新读取 `.project/.workflow/current/`、`acceptance.md`、`context.md`、`project/status.md`、`roadmap.md`、`系统模块设计方案.md`、最新 `review/2026/04/14.md` 与最近 `history-ai`，确认当前任务切换到 B3-1 合理，且应优先实现 `calculator_tool`
- 阅读 `backend/routers/chat.py`、`backend/services/llm.py`、`backend/database.py`、`backend/models.py`、`backend/serializers.py`，确认当前最小链路为“写用户消息 -> 调 LLM -> 写助手消息”，`messages.tools_used` 字段已存在但尚未真正使用
- 先新增 `backend/tests/test_calculator_tool.py`、`backend/tests/test_chat_tools_api.py`、`backend/tests/test_llm_tool_loop.py`，以 TDD 方式覆盖工具计算、非法表达式、mock 模式下 `/api/chat` 的工具使用与持久化，以及 OpenAI-compatible `tool_calls` 的两轮循环
- 新建 `backend/tools/calculator_tool.py` 和 `backend/tools/__init__.py`，使用 `ast + sympy` 的安全解析方式实现 `calculator_tool(expression: str) -> str`，不使用 `eval`
- 扩展 `backend/services/llm.py`：新增 `AssistantReply` 返回结构、`calculator_tool` 的工具定义、真实模型下的 `tools` / `tool_calls` 循环、工具执行结果回填，以及本地 mock/fallback 的同样工具主链路
- 更新 `backend/routers/chat.py`，让助手消息把 `assistant_reply.tools_used` 落库到 `messages.tools_used`，并通过现有 serializer 回传给 API 响应
- 在 `backend/requirements.txt` 中增加 `sympy` 依赖，并安装 `backend/requirements.txt`
- 运行单元测试与集成测试：`test_calculator_tool`、`test_llm_tool_loop`、`test_chat_tools_api`、`test_sessions_api` 全部通过
- 重启后端后，用真实 HTTP 请求验证 `POST /api/chat`：对“请计算 2^10 是多少？”返回结果 `1024`，并在 `assistant_message.tools_used` 中看到 `calculator_tool` 调用记录；对非法表达式“请计算 2 + ) 的结果”返回错误文本但服务不崩溃

### 改动文件
- `backend/tools/__init__.py` - 导出 `calculator_tool`
- `backend/tools/calculator_tool.py` - 新增安全数学表达式工具实现
- `backend/services/llm.py` - 新增工具定义、工具调用循环、工具感知的 mock/fallback 与结构化返回
- `backend/routers/chat.py` - 将工具调用结果写入助手消息的 `tools_used`
- `backend/requirements.txt` - 增加 `sympy` 依赖
- `backend/tests/test_calculator_tool.py` - 新增 calculator_tool 单测
- `backend/tests/test_llm_tool_loop.py` - 新增 OpenAI-compatible tool loop 单测
- `backend/tests/test_chat_tools_api.py` - 新增 `/api/chat` 工具调用集成测试
- `.project/.workflow/history-full/2026/04/14.md` - 追加本轮工作日志

### 关键决策
- 真实模型路径和 mock/fallback 路径同时接入 `calculator_tool`：这样即使本机没有配置真实大模型 Key，也能按 acceptance 自主验证工具主链路
- `calculator_tool` 采用 `ast` 白名单解析后交给 `sympy` 计算，而不是直接使用 `eval` 或不安全的字符串执行方式，满足任务的安全约束
- 工具调用情况暂时只写入助手消息的 `tools_used` 字段，不额外落独立 `tool` 消息行，保持本轮改动最小且满足当前验收

### 遇到的问题
- 当前环境下运行带 SQLite 临时库的测试会在沙箱内触发 `disk I/O error`，因此最终测试通过提权环境运行 `unittest` 完成
- 当前 `.venv` 初始未安装 `sympy`，导致工具与相关测试无法导入；安装 `backend/requirements.txt` 后恢复正常
- `node --test` 与部分 SQLite 写盘在当前环境都有额外限制，因此本轮严格区分“纯逻辑单测”和“需要提权的集成验证”路径

### 当前结果
- `calculator_tool` 已可独立计算数学表达式，并在非法输入时返回错误字符串而不是抛异常
- `/api/chat` 已能在工具主链路中记录 `calculator_tool` 调用，助手响应中可返回正确的数学结果，且 `assistant_message.tools_used` 会带出工具名、参数、结果和状态
- 后端服务可正常启动，现已通过真实 HTTP 请求验证“计算 2^10 -> 1024”和“非法表达式不崩”两条主链路

## 任务：B3-1 review 修复（calculator_tool 自然语言幂表达式）

### 做了什么
- 读取最新 review 日志，确认阻塞点是 mock / fallback 路径未覆盖“计算 2 的 10 次方”这类中文自然语言问法
- 先在 `backend/tests/test_llm_tool_loop.py` 新增 mock 模式回归测试，再在 `backend/tests/test_chat_tools_api.py` 新增 `/api/chat` 集成回归测试，验证中文幂表达式会触发 `calculator_tool`
- 扩展 `backend/services/llm.py` 中的表达式提取逻辑：补充数学问句前缀清理，并把“X 的 Y 次方 / 次幂”标准化为 `({X})^({Y})`
- 重新运行 `test_llm_tool_loop` 指定回归测试、`test_chat_tools_api` 指定回归测试，以及完整 backend unittest 套件
- 重启后端并用真实 HTTP 分别验证“请计算 2^10 是多少？”和“计算 2 的 10 次方”两条请求，确认都返回 `1024` 且 `tools_used` 记录 `calculator_tool`

### 改动文件
- `backend/services/llm.py` - 修复自然语言幂表达式识别
- `backend/tests/test_llm_tool_loop.py` - 新增 mock 模式中文幂表达式回归测试
- `backend/tests/test_chat_tools_api.py` - 新增 `/api/chat` 中文幂表达式集成回归测试

### 关键决策
- 只补 review 明确指出的“X 的 Y 次方 / 次幂”识别，不扩大到更多自然语言数学解析，避免本轮修复范围失控
- 先做单测红灯，再做 API 回归，再做真实 HTTP 验证，保持修复证据链完整
- 真实 HTTP 验证中使用 Unicode 转义请求体，绕过当前终端中文编码噪音，确保验证的是后端逻辑而不是 shell 编码副作用

### 遇到的问题
- 沙箱内运行带 SQLite 临时库的 API 测试仍会触发 `disk I/O error`，因此该条回归测试改在提权环境下执行
- PowerShell 直接发送中文请求体时会受到当前终端编码影响，初次请求出现问句内容被替换为 `?` 的现象；改用 Unicode 转义后恢复正常验证

### 当前结果
- review 阻塞问题已修复：mock / fallback 路径现在可识别“计算 2 的 10 次方”并调用 `calculator_tool`
- backend unittest 现为 10/10 通过
- 真实 HTTP 已验证两条问法都能返回 `1024`，后端当前保持运行
