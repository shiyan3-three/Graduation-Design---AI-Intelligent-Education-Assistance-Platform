# Review 2026-04-14

## F2-1 前端最简对话页发送与展示

### 结论
- [x] 通过

### 阻塞问题
- [ ] 无

### 非阻塞问题
- [ ] `.project/.workflow/history-full/2026/04/14.md` 同日还记录了“Windows 一键启动/关闭脚本”工作，但不在 `current/task.md` 的 F2-1 范围内。本次通过判断只基于前端对话页相关文件，建议由 `Leader Agent` 后续确认该脚本工作是否需要独立立项或单独收口。
- [ ] 本轮未收到人类浏览器操作反馈，审查主要依赖命令行 HTTP 联调与 Node DOM stub 冒烟；如需进一步确认视觉细节与手工交互体验，可补一轮人类点击验证。

### 是否满足 acceptance
- [x] 是
- [x] 前端服务可正常启动：`project-scipt/start-frontend.bat` 拉起后，`http://127.0.0.1:5173/` 返回 `200`
- [x] 页面存在消息输入框和发送按钮：首页 HTML 实际包含 `#message-input` 与 `#send-button`
- [x] 请求参数格式符合 `api-design.md` 第 5.3 节：静态审查 `frontend/src/chat-state.js` 与 `frontend/src/app.js`，请求体为 `{ content, session_id }`
- [x] 后端正常返回后，AI 回复可进入前端展示链路：经 `http://127.0.0.1:5173/api/chat` 实际联调成功，并通过 Node DOM stub 执行 `frontend/src/app.js` 验证用户消息与助手回复都被渲染进消息列表
- [x] 对话消息可以区分用户与 AI：`frontend/styles.css` 区分 `role-user` / `role-assistant`，DOM 冒烟也验证了列表渲染结果
- [x] 存在基础加载态：`npm test` 通过（5/5），其中 `getViewModel` 用例覆盖了发送中按钮禁用/文案变化
- [x] 请求失败时有基础错误提示：通过无效 `session_id` 走前端代理实际拿到 `404 SESSION_NOT_FOUND`，同时 DOM stub 验证错误文案会写入 `#error-banner`
- [x] 未发现影响主链路的明显运行异常：`npm test` 通过，前端首页可访问，成功/失败主链路都已命令行验证

### open-issues 补充
- 当前未发现必须新增到 `project/open-issues.md` 的阻塞风险。
- 若 `Leader Agent` 认为“同日存在未登记到 `current/task.md` 的脚本工作”属于流程风险，可作为协作流程问题再决定是否补录。

### 建议下一步
1. `Leader Agent` 可以按“通过”收口 F2-1，更新 `project/status.md` 并推进下一任务。
2. 如需更强证据，可补一轮人类浏览器点击验证，重点确认页面视觉与实际交互手感。
3. `Leader Agent` 可确认“Windows 一键启动/关闭脚本”是否应作为独立任务登记，避免 `history-full` 与 `current/task.md` 的范围歧义。

## F2-1 补充复审（基于人类运行反馈）

### 结论
- [x] 通过

### 阻塞问题
- [ ] 无

### 非阻塞问题
- [ ] 人类反馈暂未直接贴出第 1 轮响应体 `data.session.id`、`data.assistant_message.content` 和浏览器 Console 截图；不过结合前序命令行联调、DOM 冒烟和本轮真实浏览器请求链路，当前不足以构成阻塞。
- [ ] `.project/.workflow/history-full/2026/04/14.md` 同日仍包含“Windows 一键启动/关闭脚本”记录，建议由 `Leader Agent` 后续决定是否单独立项收口。

### 是否满足 acceptance
- [x] 是
- [x] 人类已在真实浏览器打开 `http://localhost:5173/`，确认前端页面可访问并真实发出 `/api/chat` 请求
- [x] 第 1 轮请求体为 `{"content":"1","session_id":null}`，符合首轮不带会话 ID 的要求
- [x] 第 2、3 轮请求体分别带有 `{"content":"2","session_id":7}`、`{"content":"3","session_id":7}`，确认前端已从首轮后维护并复用固定 `session_id`
- [x] 请求侧字段名 `content`、`session_id` 与 `.project/api-design.md` 第 5.3 节契约一致
- [x] 结合上一节 review 中的命令行验证，AI 回复展示、错误提示、基础加载态和消息区分也都已有独立证据支撑
- [x] 目前未收到任何“页面无法展示”“字段错误”或“主链路被 Console 报错打断”的人类反馈，结合静态审查与运行验证，可维持通过结论

### open-issues 补充
- 当前无需新增阻塞风险到 `project/open-issues.md`。

### 建议下一步
1. `Leader Agent` 可以直接基于本 review 和本轮人类运行反馈收口 F2-1。
2. 如果团队希望把证据链做得更完整，可额外归档一份首轮响应体和浏览器 Console 截图，但这不是当前通过的前置条件。

## F2-侧栏 对话历史侧栏的最小占位或基础列表

### 结论
- [x] 通过

### 阻塞问题
- [ ] 无

### 非阻塞问题
- [ ] `current/context.md` 中对 `GET /api/sessions` 的示例仍写成了 `data.sessions`，而实际后端返回与前端实现使用的是 `data.items`。本轮实现已经按真实接口完成联调，不影响通过，但建议由 `Leader Agent` 后续收口文档，避免后续任务再次被过时示例误导。

### 是否满足 acceptance
- [x] 是
- [x] 页面存在侧栏区域，且页面加载后会调用 `GET /api/sessions` 并展示会话列表：命令行访问 `http://127.0.0.1:5173/` 返回 `200`，首页 HTML 包含 `session-list`；同时通过前端代理请求 `GET /api/sessions` 实测拿到 7 条历史会话
- [x] 侧栏每条会话显示会话标题：`GET /api/sessions` 返回的列表中可读到标题字段，页面骨架与脚本实现均按 `title` 渲染
- [x] 点击侧栏某条会话会调用 `GET /api/sessions/{id}` 并切换主区消息：人类真实浏览器反馈已确认“点击任一旧会话，主区消息会切换”；命令行复核中，对旧会话 `id=2` 调用 `GET /api/sessions/2` 返回 2 条消息；DOM 冒烟也验证了实际请求序列包含 `GET /api/sessions/2`
- [x] 发送新消息后，侧栏列表会更新：人类反馈已确认“发送一条消息，会话会刷新到顶部，没有报错”；命令行复核中，对旧会话 `id=2` 发送消息后，列表首项变为 `id=2`；另一次以 `session_id=null` 发起新会话后，新会话 `id=8` 也出现在侧栏顶部
- [x] 前端服务可正常启动，页面无明显阻塞性控制台报错：`npm test` 通过（前端 10/10 测试通过），`5173`/`8000` 均处于监听状态；人类反馈明确说明操作过程中“没有报错”

### open-issues 补充
- 当前未发现需要新增到 `project/open-issues.md` 的阻塞风险。
- 若团队认为 `context.md` 的接口示例过时会持续干扰协作，可由 `Leader Agent` 在后续收口时作为流程类问题处理。

### 建议下一步
1. `Leader Agent` 可以按“通过”收口 F2-侧栏，并将 F2 阶段标记为完成。
2. 建议同步修正 `current/context.md` 中过时的 `data.sessions` 示例，减少后续任务的上下文偏差。
3. 下一步可以进入第 3 阶段 B3（工具能力实现与验证）。

## B3-1 后端接入 `calculator_tool`（预备阶段）

### 结论
- [ ] 未通过

### 阻塞问题
- [ ] `backend/services/llm.py:94-123, 261-282` - 当前 mock / fallback 路径只会在用户问题被 `_extract_expression_from_text()` 识别成“公式形表达式”时调用 `calculator_tool`。我已用真实 HTTP 验证：`POST /api/chat` 发送“请计算 2^10 是多少？”时，助手回复包含 `1024` 且 `tools_used[0].tool_name == "calculator_tool"`；但发送验收标准示例“计算 2 的 10 次方”时，响应退回为普通演示文案，`assistant_message.tools_used` 为 `null`，没有调用工具，也没有返回 `1024`。这意味着 acceptance 第 4 条当前未满足。建议优先扩展自然语言表达式抽取逻辑（至少覆盖“X 的 Y 次方”这类问法），或让 mock 路径在该问法下也稳定走工具链路。

### 非阻塞问题
- [ ] 本轮 `unittest` 首次在沙箱内执行时出现 SQLite 临时库 `PermissionError` / `disk I/O error`，在提权环境下重跑后 8 个测试全部通过。该现象更像当前运行环境限制，不作为本任务阻塞项，但建议后续统一测试落盘路径或执行方式，减少审查噪音。

### 是否满足 acceptance
- [ ] 否
- [x] `backend/tools/calculator_tool.py` 已存在，且导出了 `calculator_tool(expression: str) -> str`
- [x] 工具实现使用了 `sympy`，未使用 `eval`
- [x] 非法表达式不会导致服务崩溃：真实 HTTP 请求“请计算 2 + ) 的结果”返回 `200`，助手文本包含错误信息，`tools_used[0].status == "error"`
- [ ] `/api/chat` 对验收示例“计算 2 的 10 次方”尚未返回正确结果 `1024`
- [x] `sympy` 已加入 `backend/requirements.txt`
- [x] 后端服务可正常启动，`127.0.0.1:8000` 可监听；提权环境下 `unittest` 8/8 通过

### open-issues 补充
- 若后续仍长期依赖 mock / fallback 路径做本地联调，建议把“自然语言数学问法覆盖不足”作为工具阶段的持续风险由 `Leader Agent` 视情况同步到 `project/open-issues.md`。

### 建议下一步
1. `Coding Agent` 先修复 mock / fallback 路径对“计算 2 的 10 次方”这类自然语言问法的识别，让该问法稳定触发 `calculator_tool` 并返回 `1024`。
2. 补一条回归测试，直接覆盖验收示例或等价中文自然语言问法，避免只测 `2^10` 这种公式形输入。
3. 修复后重新运行 `unittest` 与两条 HTTP 用例：`请计算 2^10 是多少？`、`计算 2 的 10 次方`。

## B3-1 补充复审（修复后）

### 结论
- [x] 通过

### 阻塞问题
- [ ] 无

### 非阻塞问题
- [ ] 本轮 `unittest` 仍需在提权环境下运行，原因是当前沙箱对 SQLite 临时库写入有限制；这更像环境约束，不作为本任务阻塞项。

### 是否满足 acceptance
- [x] 是
- [x] `backend/tools/calculator_tool.py` 存在，实现了 `calculator_tool(expression: str) -> str`
- [x] 工具实现使用 `sympy`，未使用 `eval`
- [x] 非法表达式输入不会导致服务崩溃：`请计算 2 + ) 的结果` 返回 `200`，助手消息包含错误文本，且 `tools_used[0].status == "error"`
- [x] `/api/chat` 已接入工具调用：真实 HTTP 复测中，`请计算 2^10 是多少？` 与验收示例 `计算 2 的 10 次方` 都返回了包含 `1024` 的助手回复，且 `assistant_message.tools_used[0].tool_name == "calculator_tool"`
- [x] `sympy` 已加入 `backend/requirements.txt`
- [x] 后端服务可正常启动，无明显阻塞性报错：`127.0.0.1:8000` 处于监听状态，`unittest` 在提权环境下 10/10 通过

### open-issues 补充
- 当前无需新增到 `project/open-issues.md` 的阻塞风险。

### 建议下一步
1. `Leader Agent` 可以按“通过”收口 B3-1，并推进 B3-2。
2. 如后续继续依赖沙箱内运行单测，可统一调整测试落盘目录或执行策略，减少环境噪音。
