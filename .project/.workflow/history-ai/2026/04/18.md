# 2026-04-18

## 当日关键进展

- 完成 M4-2 全部工具 MCP 封装 + 调用路径切换（Review 通过），第 4 阶段 MCP Server 正式接入全部完成。
- 完成 FR-1 前端 React 重构（Vite + React）：浅色专业暗台风格，含深色模式切换，Review 三轮通过，人类浏览器验证通过。
- 主要 React 组件：`App.jsx`、`SidebarReact.jsx`、`ChatPanelReact.jsx`、`InputBarReact.jsx`、`MessageBubbleReact.jsx`、`ThemeToggleReact.jsx`。
- 实施 UI/UX 精细打磨：全局色彩规范 Design Tokens、Markdown 渲染（`react-markdown` + `remark-gfm`）、textarea 高度自适应。
- 修复 `question_tool` 本地匹配增强：知识点关键词权重匹配 + 同学科兜底，LLM 转站 provider failover。
- 新增 `backend/services/llm_provider_support.py` 支持 `LLM_PROVIDER_CONFIGS` / `LLM_BASE_URLS` 多 provider 自动切换。
- 修复 MCP 子进程环境变量未继承问题：`mcp_client.py` 传递 `os.environ.copy()` 到 stdio 连接。
- 修复前端防抖和可恢复性问题：textarea disabled 状态、Enter 键拦截、`isSending`/`isLoadingSessionDetail` 前置检查。
- 修复 Tool Suggestion 卡片按钮文本改为使用 `getToolLabel` 显示标准中文标签。

## 关键决策

- 前端技术栈从 Vanilla JS 迁移到 React（Vite + React）→ 第 5 阶段起需批量新建页面，React 效率更高。
- 默认定为浅色专业暗台风，同时保留一键切换深色模式展示适配能力。
- 先复用原有业务逻辑层（`api.js`、`chat-state.js`），React 重构时业务逻辑尽量一字不动。
- MCP server 子进程必须显式传递父进程环境变量，确保 LLM provider 和 key 一致。
- provider failover 按"Provider 列表"模型，只轮询 `base_url`，安全支持不同 key 和 model 组合。

## 当前遗留问题

- React 组件交互自动化测试覆盖偏薄，FR-1 仅靠命令行 + 人类浏览器验证。
- MCP Client 每次调用新建 stdio 连接（功能正确，性能优化暂不立项）。
- 当前任务已切到 B1（后端用户注册与登录接口），Leader Agent 已下达指令。

## 给下一个 Agent 的交接

- 第 4 阶段 MCP 和 FR-1 React 重构已全部完成，项目进入第 5 阶段用户体系。
- 当前任务 B1：实现 `POST /api/register` 和 `POST /api/login`，使用 bcrypt + PyJWT。
- 前端已迁移到 `frontend/src/` 下 React 组件结构，`npm run dev` 启动 Vite 开发服务器。
- 后端有 3 个真实 LLM provider 配置在 `backend/.env`，支持 DeepSeek / Kimi / GLM 自动 failover。
- `npm test` 25 项通过，`npm run build` 正常产出 `dist/`。
- 后端 36+ 项测试通过。
