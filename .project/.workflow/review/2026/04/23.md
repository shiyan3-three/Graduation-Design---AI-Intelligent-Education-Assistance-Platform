# Review 2026-04-23

## B-Admin 后端管理员模块 Review

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `backend/database.py:158` - `ensure_test_admin(connection)` 当前随 `init_database()` 无条件执行，会在任意初始化环境注入固定 `id=9999` 的管理员账号。当前 B-Admin 验收依赖该测试管理员，且账号密码哈希不可直接登录，不阻塞本轮；但后续如进入部署或演示环境，建议由 Leader 决定是否改为显式 dev/test 开关、初始化脚本或独立 seed 流程。
- [ ] 当前无法使用 `git diff` 审查未提交差异，因为工作区不是 Git 仓库；本轮改用定向读文件、测试和 TestClient 烟测完成审查。

## 是否满足 acceptance
- [x] 是
- [x] `users` 表已有 `is_admin` 字段；旧 schema 临时库经 `init_database()` 后成功迁移，重复初始化后 `is_admin` 仍只有 1 列
- [x] `GET /api/admin/users` 无 Token 返回 401
- [x] `GET /api/admin/users` 普通用户 Token 返回 403
- [x] `GET /api/admin/users` 管理员 Token 返回 200，列表含 `is_admin` 字段
- [x] `GET /api/admin/stats` 管理员 Token 返回 200，包含 `total_users`、`total_sessions`、`total_messages`、`total_recommendations`
- [x] `DELETE /api/admin/users/{id}` 管理员删除普通用户返回 200，用户已从 DB 删除
- [x] 管理员尝试删除自身返回 400
- [x] `backend/models.py` 已新增 `AdminUserOut`、`AdminUsersResponseData`、`AdminStatsResponseData`
- [x] `backend/dependencies/auth.py` 已新增 `get_current_admin_user_id`
- [x] 现有后端接口无回归，`python -m unittest discover -s backend/tests` 全部通过

## open-issues 补充
- 建议 Leader 后续评估是否把“固定测试管理员无条件注入”同步到 `project/open-issues.md`，尤其是 F-Admin 需要真实管理员登录或项目进入演示/部署环境前。

## 验证记录
- `& '.\.venv\Scripts\python.exe' -m unittest backend.tests.test_admin_api`：6/6 通过
- `& '.\.venv\Scripts\python.exe' -m unittest discover -s backend/tests`：96/96 通过
- 临时 TestClient 烟测：旧 schema 迁移后 `is_admin_count=1`，`admin_row.id=9999` 且 `is_admin=1`
- 临时 TestClient 烟测：无 Token 访问 `/api/admin/users` 返回 401，普通用户 Token 返回 403，管理员 Token 返回 200
- 临时 TestClient 烟测：`/api/admin/stats` 返回 4 个统计字段；删除自身返回 400；删除普通用户返回 200
- 临时 DB 验证：删除普通用户后，关联 `sessions`、`messages`、`recommendation_items` 均被级联删除为 0

## 建议下一步
1. Leader 可收口 B-Admin 后端管理员模块。
2. 进入 F-Admin 前，建议先决定管理员登录策略：继续使用测试管理员 JWT、手动 seed 管理员，还是扩展登录响应/用户模型携带 `is_admin`。
3. 如后续考虑部署或公开演示，建议把测试管理员注入从默认初始化流程中拆出。

---

## B-Admin-2 + F-Admin 题库 CRUD 后端 + 前端管理员面板 Review

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `backend/database.py:158` / `backend/routers/auth.py:206` - 内置 `id=9999` 管理员仍使用 `dev-only-hash`，不能通过 `/api/login` 密码登录；本轮功能在“已有 bcrypt 管理员用户”场景下可用，且测试可直接构造管理员 JWT，但真实浏览器演示 F-Admin 前仍需要手动 seed 一个可登录管理员，或由 Leader 明确管理员初始化策略。
- [ ] `frontend/src/pages/AdminPage/index.jsx:85` - AdminPage 的 `loadStats()`、`loadUsers()`、`loadQuestions()` 以及后续操作函数没有 `try/catch`；HTTP 401/403 会由 `requestJson` 处理，但网络级 `fetch` reject 会形成未处理 Promise。当前不影响验收主链路，后续建议补局部错误态。
- [ ] 尚未做真实浏览器交互验收；命令行已覆盖测试、构建、路由和后端接口烟测。建议人类补一次管理员账号登录后的 Header 菜单、Tab 切换、内联新增/编辑/删除确认。

## 是否满足 acceptance
- [x] 是
- [x] 登录接口响应中包含 `is_admin` 字段；临时烟测确认普通用户登录返回 `is_admin=false`
- [x] `PUT /api/admin/users/{id}/admin` 无 Token 返回 401，普通用户返回 403，管理员返回 200 并切换状态
- [x] `GET /api/admin/questions` 管理员 Token 返回 200，`data.items` 中包含合并后的 `options`
- [x] `POST /api/admin/questions` 缺少必填字段返回 422，管理员创建成功返回 201
- [x] `PUT /api/admin/questions/{id}` 修改后，列表查询可见更新结果
- [x] `DELETE /api/admin/questions/{id}` 删除成功后，再次删除同 ID 返回 404
- [x] 现有所有后端测试通过，`python -m unittest discover -s backend/tests` 为 104/104 OK
- [x] `GlobalHeader` 中管理员用户会显示蓝色“🛡 管理后台”菜单项，普通用户本地态不可见
- [x] `/admin` 路由中非管理员本地态会自动跳转至 `/`
- [x] 系统概览 Tab 通过 `fetchAdminStats()` 展示 `total_users`、`total_sessions`、`total_messages`、`total_recommendations`
- [x] 用户管理 Tab 展示所有用户，支持切换管理员状态和内联展开确认删除
- [x] 题库管理 Tab 展示题目，并支持学科过滤与专题搜索
- [x] 点击“+ 新增题目”会在表格顶部展开内联新增表单行
- [x] 点击“编辑”会将该行切换为内联可编辑状态，前端只修改 `question`、`answer`、`options`
- [x] 点击“删除”会在该行展开内联确认区，不使用弹层
- [x] `npm run build` 通过
- [x] `/chat`、`/`、`/learning`、`/recommend`、`/login`、`/admin` 路由烟测无回归，均返回 200

## open-issues 补充
- 建议 Leader 在最终收口前明确“管理员账号如何初始化/登录”：当前后端支持 bcrypt 管理员正常登录，但内置 `admin` 账号本身不可登录。
- AdminPage 网络异常兜底可作为后续体验增强项；不影响当前最终任务验收。

## 验证记录
- `& '.\.venv\Scripts\python.exe' -m unittest backend.tests.test_admin_questions`：8/8 通过
- `& '.\.venv\Scripts\python.exe' -m unittest discover -s backend/tests`：104/104 通过
- `npm test`：全部通过，包含 `admin-page.test.js`
- `npm run build`：通过，325 modules transformed
- 临时 TestClient 烟测：普通用户登录返回 `is_admin=false`；内置 `admin` 密码登录返回 401（符合当前 dev-only-hash 限制）
- 临时 TestClient 烟测：管理员切换接口 401 / 403 / 200 分支正确，成功后 `is_admin=true`
- 临时 TestClient 烟测：题库接口缺字段 422、创建 201、列表 200、更新 200、删除 200、二次删除 404；`options` 合并字段格式正确
- 前端路由烟测：`/`、`/chat`、`/learning`、`/recommend`、`/login`、`/admin` 均返回 200

## 建议下一步
1. Leader 可收口 B-Admin-2 + F-Admin 合并任务。
2. 最终演示前，建议准备一个真实可登录的管理员账号 seed，避免只能靠手工改 localStorage 或手工 JWT 进入后台。
3. 人类可补一次真实浏览器管理员面板烟测，重点验证 Header 管理入口、三 Tab 切换、题库内联新增/编辑/删除。

---

## 最终收口复审 - 多管理员配置与 AdminPage 网络兜底

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `backend/database.py:167` - `ADMIN_USERNAMES` 只会在 `init_database()` 执行时提升“已存在用户”为管理员；如果先启动空库、后注册 `admin1`，需要再执行一次初始化或重启后端才会提升。当前运行库已验证 `admin1.is_admin=1`，不阻塞最终演示；建议 Leader 在演示步骤中明确“先创建账号，再运行初始化/重启后端”。
- [ ] 尚未做真实浏览器管理员面板交互验收；命令行已覆盖后端、前端测试、构建、路由和运行库状态。最终演示前建议人类补一次登录 `admin1` 后进入 `/admin` 的真实浏览器烟测。

## 是否满足 acceptance
- [x] 是
- [x] 登录接口响应中包含 `is_admin` 字段；前序复审与后端回归已覆盖
- [x] `PUT /api/admin/users/{id}/admin` 权限分支和切换状态仍由后端 105/105 回归覆盖
- [x] `GET /api/admin/questions`、`POST`、`PUT`、`DELETE` 题库 CRUD 主链路仍由后端 105/105 回归覆盖
- [x] `ADMIN_USERNAMES` 支持多用户名配置，并兼容 `ADMIN_USERNAME`
- [x] 多管理员配置只提升已存在用户，不自动创建缺失用户，避免生成无可用密码管理员
- [x] `backend/.env.example` 已补充 `ADMIN_USERNAMES=admin1` 示例
- [x] 当前运行库 `project-scipt/runtime/backend_runtime.db` 已验证 `admin1.is_admin=1`
- [x] AdminPage 加载、增删改请求统一走 `runAdminRequest()`，网络 reject 会显示“网络异常，请检查后端服务是否已启动”
- [x] `npm run build` 通过
- [x] `/chat`、`/`、`/learning`、`/recommend`、`/login`、`/admin` 路由烟测无回归，均返回 200

## open-issues 补充
- 可由 Leader 视情况同步一条演示注意事项：`ADMIN_USERNAMES` 是“启动时提升已存在用户”的 seed 配置，不是管理员自动注册机制。

## 验证记录
- `& '.\.venv\Scripts\python.exe' -m unittest backend.tests.test_admin_api`：7/7 通过
- `& '.\.venv\Scripts\python.exe' -m unittest backend.tests.test_admin_questions`：8/8 通过
- `& '.\.venv\Scripts\python.exe' -m unittest discover -s backend/tests`：105/105 通过
- `node tests/admin-page.test.js`：4/4 通过
- `npm test`：全部通过，包含管理员网络异常兜底测试
- `npm run build`：通过，326 modules transformed
- 运行库验证：`db_exists=True`、`has_is_admin=True`、`admin1={'username': 'admin1', 'is_admin': 1}`
- 路由烟测：`/`、`/chat`、`/learning`、`/recommend`、`/login`、`/admin` 均返回 200

## 建议下一步
1. Leader 可收口最终任务，并更新 `project/status.md` 与 `open-issues.md`。
2. 人类最终演示前建议登录 `admin1`，确认 Header 下拉出现“🛡 管理后台”，并在 `/admin` 做一次三 Tab 真实操作烟测。
3. 若要整理交付材料，建议说明管理员账号初始化顺序：先存在账号，再通过 `ADMIN_USERNAMES` + 后端初始化提升权限。
