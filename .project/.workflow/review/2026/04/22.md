# Review 2026-04-22

## B2-增强 后端 Chat 流式输出（SSE）Review

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `backend/services/llm.py:219` - 如果真实 LLM provider 在已经输出部分 delta 后中途异常，当前顶层 `generate_assistant_reply_stream()` 会 fallback 到本地 mock stream，理论上可能让客户端收到“部分真实输出 + mock 输出”的混合内容。当前 mock/工具/正常流验收均通过，且真实 provider 中途断流不在本轮 acceptance 内，不阻塞通过；后续进入真实 LLM 流式联调时，建议改为“已输出 delta 后异常则发 error，不再 fallback”。

## 是否满足 acceptance
- [x] 是
- [x] `backend/routers/chat.py` 中已新增 `POST /api/chat/stream` 端点
- [x] 无 Token 请求返回 401，且在流开始前返回
- [x] 空 content 请求返回 400
- [x] 正常请求响应 `Content-Type` 包含 `text/event-stream`
- [x] 响应体可解析为多行 `data: <json>` SSE 格式
- [x] 事件序列包含多个 `{"type": "delta", "content": "..."}` 事件
- [x] 最后一个事件为 `{"type": "done", "session_id": <正整数>, "message_id": <正整数>, "tools_used": [...] }`
- [x] `done` 事件发出前已完成 assistant 消息落库；临时 TestClient 烟测确认 `message_id` 对应 `messages.role='assistant'` 且内容与 delta 拼接一致
- [x] `backend/services/llm.py` 已新增 `generate_assistant_reply_stream` 异步生成器
- [x] mock 模式下流式输出可见多个 delta 事件
- [x] 现有 `POST /api/chat`、`/api/sessions`、`/api/recommend`、`/api/analytics/*`、`/api/auth/*` 后端 discovery 回归通过

## open-issues 补充
- 无新增必须同步到 `project/open-issues.md` 的阻塞风险。
- 真实 LLM 模式下 token 逐步流出属于加分项；当前环境未配置真实 API key，本轮主要验证 mock、工具直达和 SSE 协议闭环。

## 验证记录
- `& '.\.venv\Scripts\python.exe' -m unittest backend.tests.test_chat_stream`：4/4 通过
- `& '.\.venv\Scripts\python.exe' -m unittest discover -s backend/tests`：86/86 通过
- 临时 TestClient SSE 烟测：状态码 200，`content-type=text/event-stream; charset=utf-8`，11 个 delta + 1 个 done，`done.session_id=1`，`done.message_id=2`，assistant 消息已落库且内容匹配

## 建议下一步
1. Leader 可收口 B2-增强 后端 Chat 流式输出（SSE）。
2. 下一阶段可进入 F2-增强，让前端消费 `/api/chat/stream` 并处理 delta/done/error 三类事件。
3. 真实 LLM key 可用时，再补一次人工 curl 或集成烟测，观察 token 是否逐步流出。

---

## F2-增强 前端流式消费 SSE 与 UX 优化 Review

## 结论
- [x] 未通过

## 阻塞问题
- [ ] `frontend/src/chat-state.js:171` - 流式请求失败时没有把任何消息标记为 `failed`。我用状态层脚本复现：`startSubmission()` 后如果在首个 `delta` 前调用 `failStreamingAssistant()`，用户消息仍是 `pending: true, failed: false`；如果已经收到部分 `delta`，`failStreamingAssistant()` 会移除 streaming assistant 气泡，但用户消息是 `pending: false, failed: false`，最终仍没有任何消息进入 failed 态。这不满足 acceptance 中“网络断开或 `error` 事件时显示错误提示，消息状态标记为 failed”。建议修复 `failStreamingAssistant()`：无 streaming 尾部时复用或等价实现 `markLatestPendingMessageAsFailed()`；有 streaming 尾部时可保留 assistant 气泡并标记 failed，或移除 assistant 后把对应用户消息标记 failed，但必须保证 UI 上能看到失败状态。

## 非阻塞问题
- [ ] `frontend/src/services/api.js:236` - SSE 解析当前按 `\n\n` 分块且每个 block 只解析一个 `data:` JSON。现有后端输出正好是单行 `data: ...\n\n`，所以本轮可用；如果后续接入标准 CRLF 或多行 `data:` SSE，需要再增强解析器。当前不阻塞本轮验收。
- [ ] `frontend/src/hooks/useChatApp.js` 和 legacy `frontend/src/sidebar-app.js` 仍保留旧 `/api/chat` 路径。本轮 React 页面已走 `useChatAppReact.js`，不影响当前路由，但后续若仍有入口引用 legacy 代码，需要统一流式路径或明确废弃。

## 是否满足 acceptance
- [ ] 否
- [x] `frontend/src/services/api.js` 已新增 `streamChatRequest`，现有 `requestJson` 等函数未被删除
- [x] `frontend/src/chat-state.js` 已新增 `startStreamingAssistant`、`appendStreamingDelta`、`finalizeStreamingAssistant`、`failStreamingAssistant`
- [x] `frontend/src/hooks/useChatAppReact.js` 发送消息已改为调用 `POST /api/chat/stream`
- [x] 命令行级验证确认大块 `delta` 会被拆成多个 UI 更新 chunk，满足“逐步出现”的实现基础
- [x] 流式过程中 `state.isSending` 维持为 true，`InputBar` textarea 和按钮会被禁用，Enter 路径也有保护
- [x] 首个 `delta` 前保留 loading dots，收到首个 `delta` 后创建 streaming 消息并隐藏 dots
- [x] streaming 消息会追加 `is-streaming` class，CSS 中已实现 blink 光标；`done` 后 `streaming=false` 光标消失
- [x] `done.session_id` 会通过 `finalizeStreamingAssistant()` 更新到 `state.currentSessionId`
- [x] `done` 后会调用 `loadSessions({ silent: true })` 刷新侧栏会话列表
- [ ] 网络断开或 `error` 事件时会显示错误提示，但消息状态没有标记为 failed
- [x] `streamChatRequest` 对 401 会清理 session 并跳转 `/login`
- [x] `npm run build` 通过
- [x] `/recommend`、`/learning`、`/`、`/login`、`/register` 路由烟测均返回 200

## open-issues 补充
- 无需新增长期 open issue；当前 blocker 是 F2-增强本轮实现问题，修复后可复审。

## 验证记录
- `npm test`：通过
- `npm run build`：通过，321 modules transformed
- 路由烟测：`/recommend`、`/learning`、`/`、`/login`、`/register` 均返回 200
- `streamChatRequest` 命令行烟测：可把 `abcdefghijklmnop` 按 4 字分片为 `abcd`、`efgh`、`ijkl`、`mnop`，并正确收到 `done.session_id=3`、`done.message_id=4`
- 状态层错误路径复现：首 delta 前失败后用户消息仍 `pending=true, failed=false`；已有 delta 后失败也没有任何 failed 消息

## 建议下一步
1. Coding Agent 修复 `failStreamingAssistant()` 的错误状态收尾，确保网络错误和 SSE `error` 都会让 UI 中至少一条相关消息显示 failed。
2. 补一条 `chat-state` 或 `stream-chat-request` 回归测试，覆盖“首个 delta 前失败”和“已有 delta 后失败”两种路径。
3. 修复后重新跑 `npm test`、`npm run build`，再进入 Review Agent 复审。

---

## F2-增强 前端流式消费 SSE 与 UX 优化复审（修复后）

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `frontend/src/services/api.js:236` - SSE 解析当前按 `\n\n` 分块且每个 block 只解析一个 `data:` JSON。现有后端输出正好是单行 `data: ...\n\n`，所以本轮可用；如果后续接入标准 CRLF 或多行 `data:` SSE，需要再增强解析器。当前不阻塞本轮验收。
- [ ] `frontend/src/hooks/useChatApp.js` 和 legacy `frontend/src/sidebar-app.js` 仍保留旧 `/api/chat` 路径。本轮 React 页面已走 `useChatAppReact.js`，不影响当前路由，但后续若仍有入口引用 legacy 代码，需要统一流式路径或明确废弃。
- [ ] 尚未做真实浏览器人工验证；命令行已覆盖状态、构建、路由和 `streamChatRequest` 分片逻辑。若要更稳，建议人类补一次长回复流式显示、滚动贴底和错误提示的浏览器烟测。

## 是否满足 acceptance
- [x] 是
- [x] `frontend/src/services/api.js` 已新增 `streamChatRequest`，现有函数未删除
- [x] `frontend/src/chat-state.js` 已新增 `startStreamingAssistant`、`appendStreamingDelta`、`finalizeStreamingAssistant`、`failStreamingAssistant`
- [x] `frontend/src/hooks/useChatAppReact.js` 发送消息已改为调用 `POST /api/chat/stream`
- [x] 命令行级验证确认大块 `delta` 会被拆成多个 UI 更新 chunk，满足“逐步出现”的实现基础
- [x] 流式过程中 `state.isSending` 维持为 true，`InputBar` textarea 和按钮会被禁用，Enter 路径也有保护
- [x] 首个 `delta` 前保留 loading dots，收到首个 `delta` 后创建 streaming 消息并隐藏 dots
- [x] streaming 消息会追加 `is-streaming` class，CSS 中已实现 blink 光标；`done` 后 `streaming=false` 光标消失
- [x] `done.session_id` 会通过 `finalizeStreamingAssistant()` 更新到 `state.currentSessionId`
- [x] `done` 后会调用 `loadSessions({ silent: true })` 刷新侧栏会话列表
- [x] 网络断开或 `error` 事件时会显示错误提示，并将最近一条用户消息标记为 `failed: true`
- [x] `streamChatRequest` 对 401 会清理 session 并跳转 `/login`
- [x] `npm run build` 通过
- [x] `/recommend`、`/learning`、`/`、`/login`、`/register` 路由烟测均返回 200

## open-issues 补充
- 无新增必须同步到 `project/open-issues.md` 的阻塞风险。
- 旧版非 React 入口仍保留 `/api/chat`，作为后续整理项即可。

## 验证记录
- `node tests/chat-state.test.js`：8/8 通过
- `npm test`：全部通过
- `npm run build`：通过，321 modules transformed
- 状态层复现：首 delta 前失败、已有 streaming 后失败，最终用户消息均为 `pending=false, failed=true`
- `streamChatRequest` 命令行烟测：`abcdefghijklmnop` 被拆为 `abcd`、`efgh`、`ijkl`、`mnop`，并正确收到 `done.session_id=3`、`done.message_id=4`
- 路由烟测：`/recommend`、`/learning`、`/`、`/login`、`/register` 均返回 200

## 建议下一步
1. Leader 可收口 F2-增强 前端流式消费 SSE 与 UX 优化。
2. 若时间允许，人类可在浏览器补一次真实长回复流式显示和滚动贴底烟测；不影响本轮 Review 通过。
3. 下一阶段可进入 FH-增强 Dashboard 接入真实后端数据。

---

## B-Analytics-增强 后端学习汇总统计接口 Review

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] 无

## 是否满足 acceptance
- [x] 是
- [x] `backend/routers/analytics.py` 已新增 `GET /api/analytics/summary` 端点
- [x] 无 Token 请求返回 401
- [x] 有效请求返回 200，响应体包含 `data.total_study_days`、`data.streak_days`、`data.recommend_complete_rate`
- [x] 新用户无数据时返回 `total_study_days=0`、`streak_days=0`、`recommend_complete_rate=null`
- [x] `total_study_days` 按当前用户 `sessions.created_at` 的不重复日期数计算
- [x] `streak_days` 按今天或昨天起向前连续学习日期计算，最近学习在 2 天前时返回 0
- [x] `recommend_complete_rate` 按 `completed/(completed+skipped)*100` 计算，忽略 `pending`；无 completed/skipped 时返回 `null`
- [x] `backend/models.py` 已新增 `AnalyticsSummaryResponseData`
- [x] 原有 `GET /api/analytics/tags` 和 `GET /api/analytics/report` 无回归
- [x] 后端完整测试通过

## open-issues 补充
- 无新增必须同步到 `project/open-issues.md` 的风险。

## 验证记录
- `& '.\.venv\Scripts\python.exe' -m unittest backend.tests.test_analytics_api`：9/9 通过
- `& '.\.venv\Scripts\python.exe' -m unittest discover -s backend/tests`：90/90 通过
- 临时 TestClient 烟测：无 token 返回 401；构造 3 个学习日期、1 completed、1 skipped、1 pending 后，`/api/analytics/summary` 返回 `total_study_days=3`、`streak_days=2`、`recommend_complete_rate=50`
- 同一烟测中 `/api/analytics/tags` 与 `/api/analytics/report` 均返回 200

## 建议下一步
1. Leader 可收口 B-Analytics-增强 后端学习汇总统计接口。
2. 下一阶段可进入 FH-增强 Dashboard 接入真实后端数据，将 summary 指标接入首页 MetricCard。

---

## FH-增强 Dashboard 接入真实后端数据 Review

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] 尚未做真实浏览器人工视觉验收；命令行已覆盖前端测试、生产构建、路由访问和 Dashboard 5 个后端 API 调用路径。若要更稳，建议人类后续补一次首页真实数据展示、空态和深浅色视觉烟测。

## 是否满足 acceptance
- [x] 是
- [x] `frontend/src/services/api.js` 已新增 `fetchSessions()` 与 `fetchAnalyticsSummary()`，原有 `requestJson`、`fetchAnalyticsTags()`、`fetchAnalyticsReport()`、`fetchRecommend()` 未被删除
- [x] `HomePage` 加载时通过 `Promise.allSettled` 并行请求 `analytics/tags`、`analytics/summary`、`analytics/report`、`sessions`、`recommend`
- [x] 数据监控板累计学习天数读取 `summary.total_study_days`，单位为“天”
- [x] 已掌握知识点读取 `tags.items.length`
- [x] 推荐完成率读取 `summary.recommend_complete_rate`，有值时显示 `%`，`null` 时显示 `--`
- [x] 连续学习天数读取 `summary.streak_days`，单位为“天”
- [x] 薄弱点诊断区读取 `report.focus_areas`，加载中显示骨架屏
- [x] `focus_areas` 为空时显示空态文案，不再展示静态假数据
- [x] 靶向推荐任务读取 `recommend.items` 中前 2 条 `pending` 项，并展示题目标题
- [x] 无 pending 推荐时显示空态文案和跳转 `/chat` 的按钮
- [x] 最新学习动态读取 `/api/sessions` 前 4 条，展示标题和时间
- [x] 无 sessions 时显示空态文案和跳转 `/chat` 的按钮
- [x] 薄弱点、推荐任务、学习动态在加载中均显示骨架屏
- [x] 单个 API 失败不会影响其他区块渲染，`allSettled` 会让失败区块降级为空数据
- [x] `npm run build` 通过
- [x] `/chat`、`/learning`、`/recommend`、`/login`、`/register` 路由烟测无回归，均返回 200

## open-issues 补充
- 无新增必须同步到 `project/open-issues.md` 的阻塞风险。
- Dashboard 对失败接口目前是局部降级为空态，而不是明确展示局部错误提示；当前满足“一个 API 失败不影响其他区块”的验收，后续如要提升用户可感知性，可再补局部错误态。

## 验证记录
- `npm test`：全部通过，包含 `home-dashboard.test.js`
- `npm run build`：通过，322 modules transformed
- `node tests/home-dashboard.test.js`：3/3 通过
- 路由烟测：`/`、`/chat`、`/learning`、`/recommend`、`/login`、`/register` 均返回 200
- API helper 烟测：`fetchAnalyticsTags()`、`fetchAnalyticsSummary()`、`fetchAnalyticsReport()`、`fetchSessions()`、`fetchRecommend()` 均发起请求，并统一带上 `Authorization: Bearer review-token`
- 静态排查：未发现旧 Dashboard 假数据文案继续残留在首页渲染逻辑中

## 建议下一步
1. Leader 可收口 FH-增强 Dashboard 接入真实后端数据。
2. 人类如有时间，可补一次真实浏览器首页视觉烟测，重点看真实数据、空态、骨架屏和深浅色切换。
3. 下一阶段可以继续推进后续 Admin 或学习记录增强任务。
