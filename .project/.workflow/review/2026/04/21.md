# Review 2026-04-21

## B4 后端学习分析（标签打标 + 分析接口）复审

## 结论
- [ ] 未通过

## 阻塞问题
- [ ] `backend/routers/chat.py:110` - 当前标签提取虽然失败会被静默吞掉，但仍在 `/api/chat` 返回前同步 `await _tag_assistant_message_safely(...)`。我用一个 1.1 秒的慢标签提取 mock 复现到 `/api/chat` 响应耗时约 1.136 秒，说明标签打标仍然会阻塞主对话返回；如果真实 LLM provider 慢或超时，用户聊天响应会被额外拖住。这与 acceptance 中“标签提取失败不影响对话正常返回（不阻塞主流程）”以及 context 中“标签打标不能阻塞主对话流程”的约束不一致。建议改为 FastAPI `BackgroundTasks`、队列任务，或至少在响应返回后异步落标签，并补慢打标不拖慢响应的回归测试。

## 非阻塞问题
- [ ] `backend/tests/test_analytics_api.py` 当前覆盖了标签提取异常时仍返回 200，但没有覆盖“慢标签提取不阻塞响应”的时延边界；修复 blocker 时建议补上这一条，避免后续又把打标逻辑挪回主链路。

## 是否满足 acceptance
- [ ] 否

## open-issues 补充
- B4 的接口形态、`focus_areas` 字段、按当前用户过滤、标签去重、连接关闭和失败不返回 500 均已验证通过。
- 当前剩余问题集中在“非阻塞”的语义：功能结果正确，但打标仍在主请求链路内等待。

## 建议下一步
1. 将 `/api/chat` 的标签提取从主响应链路中移出，优先使用 FastAPI `BackgroundTasks` 在响应后写入 `message_tags`。
2. 保持现有“标签提取失败静默跳过”的保护，不要让后台任务异常影响 chat 返回。
3. 补一条慢标签提取回归测试，确认 `/api/chat` 不会等待标签任务完成。

---

## B4 后端学习分析（标签打标 + 分析接口）复审（修复后）

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `frontend/src/pages/HomePage/index.jsx` / 后续 F3 页面仍需单独接入 B4 analytics 数据；本轮 B4 不要求前端页面，不影响通过。

## 是否满足 acceptance
- [x] 是
- [x] `/api/chat` 对话后可写入 `message_tags`
- [x] 标签提取失败不影响对话正常返回
- [x] 标签提取已从主响应链路移出，改为 `BackgroundTasks` 调度
- [x] `backend/routers/analytics.py` 存在
- [x] `GET /api/analytics/tags` 返回 `{ success, message, data: { items: [{ tag, count }] } }`
- [x] `GET /api/analytics/report` 返回 `{ success, message, data: { focus_areas: [{ tag, count, rank }] } }`
- [x] 字段名使用 `focus_areas`，未使用 `weak_points`
- [x] `backend/main.py` 已注册 analytics 路由
- [x] 现有 chat/sessions/auth 功能无回归
- [x] 后端测试通过，服务启动与真实烟测由人类反馈确认

## open-issues 补充
- 本轮修复将打标逻辑改为 `BackgroundTasks`，解决了上一轮“慢标签提取拖住 `/api/chat` 返回”的阻塞问题。
- `TestClient` 对 background task 的耗时行为与真实 ASGI 响应时机不完全等价；本轮已通过直接路由级测试确认 `post_chat()` 只调度任务、不 await 打标函数。

## 建议下一步
1. Leader 可收口 B4 后端学习分析。
2. 下一阶段可进入 F3-1 前端学习记录页，使用 `/api/analytics/tags` 与 `/api/analytics/report` 做展示。

---

## F3-1 前端学习记录页与基础统计展示复审

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `frontend/src/pages/LearningPage/index.jsx:11` - 当前 `Promise.all([fetchAnalyticsReport(), fetchAnalyticsTags()])` 只有 `then/finally`，没有 `catch` 或错误态。正常 401 会由 `requestJson` 全局处理，接口返回空数据也能展示空态；但如果 fetch 发生网络级 reject，页面会产生未处理 Promise rejection。建议后续补 `error` state 或至少 catch 后展示“学习记录加载失败，请稍后重试”。
- [ ] F3-1 暂未新增前端组件/路由级自动化测试；本轮 `npm test` 仍是既有测试集合。当前通过命令行和路由烟测已足够支撑验收，但后续继续迭代学习记录页时建议补一条 `/learning` 页面渲染与空态/有数据态测试。

## 是否满足 acceptance
- [x] 是
- [x] `frontend/src/pages/LearningPage/index.jsx` 存在
- [x] 访问 `/learning` 路由能返回页面入口，无 404
- [x] 页面调用 `GET /api/analytics/report` 并读取 `focus_areas` 展示热点 Top3
- [x] 页面调用 `GET /api/analytics/tags` 并读取 `items` 展示标签频次列表
- [x] analytics API 调用复用 `requestJson`，已验证会携带 `Authorization: Bearer <token>`
- [x] 无数据时展示空态，标签区包含跳转 `/chat` 的引导按钮
- [x] 请求期间有骨架屏加载态
- [x] `frontend/src/services/api.js` 已新增 `fetchAnalyticsTags` 和 `fetchAnalyticsReport`
- [x] `frontend/src/App.jsx` 已在 `ProtectedRoute > AppLayout` 下注册 `/learning`
- [x] `GlobalHeader.jsx` 中“学习记录”已改为指向 `/learning` 的 `NavLink`，并复用 active 高亮样式
- [x] `npm run build` 通过
- [x] `/chat`、`/`、`/login`、`/register` 路由烟测均返回 200，未见路由回归

## open-issues 补充
- 本轮学习记录页已经接入 B4 analytics 数据，Dashboard 静态数据问题仍不属于 F3-1 范围。

## 建议下一步
1. Leader 可收口 F3-1 前端学习记录页与基础统计展示。
2. 下一阶段可进入 B5-1 后端推荐接口。
3. 若后续继续增强学习记录页，建议优先补错误态和组件级回归测试。

---

## B5-1 后端推荐接口复审

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `backend/routers/recommend.py:76` - 首次无 pending 推荐时会同步调用 `question_tool(topic)` 生成题目；如果本地题库未命中且真实 LLM provider 较慢，接口响应会等待较久。该行为与本轮“接口生成并返回”的任务设计一致，且交接摘要已作为已知风险说明，本轮不作为 blocker。

## 是否满足 acceptance
- [x] 是
- [x] `backend/routers/recommend.py` 存在
- [x] `GET /api/recommend` 无 Token 返回 401
- [x] 当前用户无学习标签时返回 `{ success: true, message: "ok", data: { items: [] } }`
- [x] 当前用户有标签时返回 1-3 道推荐题目，每题包含 `id`、`recommended_topic`、`question_payload`、`source`、`status`、`created_at`、`feedback_at`
- [x] `question_payload` 响应时为结构化 object，`options` 为 dict，不是字符串或数组
- [x] 首次请求后会写入 `recommendation_items`，`status='pending'`
- [x] 再次请求存在 pending 时直接复用，不重复调用 `question_tool`
- [x] `question_tool` 返回 `出题错误...` 时会静默跳过失败知识点，不影响接口返回
- [x] `backend/main.py` 已注册 recommend router
- [x] 现有 `/api/chat`、`/api/sessions`、`/api/analytics/*`、`/api/auth/*` 后端 discovery 回归通过

## open-issues 补充
- 本轮推荐接口符合 B5-1 范围；`POST /api/recommend/feedback` 尚未实现，按 task.md 属于 B5-2 后续任务。

## 建议下一步
1. Leader 可收口 B5-1 后端推荐接口。
2. 下一阶段可进入 B5-2 推荐反馈接口。
3. 若后续真实 LLM provider 响应较慢，再考虑为推荐生成引入后台预生成或超时降级策略。

---

## B5-2 后端推荐反馈接口复审

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] 无

## 是否满足 acceptance
- [x] 是
- [x] `backend/routers/recommend.py` 中已新增 `POST /api/recommend/feedback`
- [x] 无 Token 请求返回 401
- [x] 请求体 `status='pending'` 等非法值返回 400
- [x] 请求不存在或不属于当前用户的 `id` 返回 404
- [x] 对已为 `completed` 的题目再次提交反馈返回 409
- [x] 对 `pending` 题目提交 `completed` 返回 200，响应 `data.status='completed'` 且 `feedback_at` 不为 null
- [x] 对 `pending` 题目提交 `skipped` 返回 200，响应 `data.status='skipped'` 且 `feedback_at` 不为 null
- [x] 响应 `data` 包含完整题目字段：`id`、`recommended_topic`、`question_payload`、`source`、`status`、`created_at`、`feedback_at`
- [x] `question_payload` 响应时为结构化 object
- [x] `backend/models.py` 已新增 `FeedbackRequest`，并用 `Literal['completed', 'skipped']` 做合法值校验
- [x] 现有 `GET /api/recommend`、`/api/chat`、`/api/sessions`、`/api/analytics/*`、`/api/auth/*` 后端 discovery 回归通过

## open-issues 补充
- 暂无新增风险。

## 建议下一步
1. Leader 可收口 B5-2 后端推荐反馈接口。
2. 下一阶段可进入 F4-1 前端推荐题目页。

---

## F4-1 前端推荐题目页 Review

## 结论
- [x] 未通过

## 阻塞问题
- [ ] `frontend/src/pages/RecommendPage/index.jsx:211` - 完成反馈成功后，当前实现只把左侧任务条改为 `✔ + topic`，没有展示 acceptance 明确要求的状态 badge 文案“已完成”。我用静态检索确认 `RecommendPage` 中没有任何“已完成”渲染文本；因此即使接口状态已更新为 `completed`，页面证据仍不满足“半透明 + 状态 badge 已完成”的验收项。建议在任务卡或详情区补状态 badge，并对 `completed` / `skipped` 分别显示可读状态（例如“已完成”“已跳过”）。
- [ ] `frontend/src/pages/RecommendPage/index.jsx:258` - 答案解析直接使用 `dangerouslySetInnerHTML` 渲染 `question_payload.explanation`。该字段来自题库或 LLM/tool 生成内容，不能按可信 HTML 处理；如果 explanation 中出现 `<img onerror=...>` 等内容，前端会把它注入 DOM，存在 XSS 风险。建议改为普通文本渲染（配合 `white-space: pre-wrap`），或使用 `react-markdown` 默认转义 HTML 的方式渲染，并补一条恶意 HTML 不会执行/不作为 HTML 注入的回归测试。

## 非阻塞问题
- [ ] `frontend/src/pages/RecommendPage/index.jsx:19` - `fetchRecommend().then(...).finally(...)` 没有 `catch` 或错误态；网络级 reject 时会出现未处理 Promise rejection，并且页面最终可能误显示“暂无推荐题目”。正常 401 已由 `requestJson` 全局处理，本项不阻塞当前验收，但建议补 `error` state，展示“推荐题目加载失败，请稍后重试”。
- [ ] `frontend/package.json:9` - 当前 `npm test` 仍没有覆盖 `RecommendPage` 的加载态、空态、完成/跳过反馈、全部完成态和 XSS 渲染边界。本轮通过静态审查与构建能发现问题，后续修复时建议补推荐页组件级回归测试，避免同类验收项再次漏掉。

## 是否满足 acceptance
- [x] 否
- [x] `frontend/src/pages/RecommendPage/index.jsx` 存在
- [x] `/recommend` 路由已注册在 `ProtectedRoute > AppLayout` 下，路由烟测返回 200
- [x] 页面调用 `GET /api/recommend`，并通过 `requestJson` 自动携带 Bearer token
- [x] loading skeleton 存在，且至少包含 1 张卡片骨架
- [x] 空态存在，并包含跳转 `/chat` 的引导
- [x] 有数据时展示 topic、题干、选项列表和操作按钮
- [x] 点击选项会进入 selected 视觉状态
- [x] 查看/收起答案按钮会切换答案区域
- [ ] `completed` 成功后缺少 “已完成” 状态 badge 文案
- [x] 跳过按钮会提交 `status: "skipped"` 并更新本地 skipped 状态
- [x] 所有卡片处理后展示“全部完成”区域，并提供“刷新推荐”按钮
- [x] `frontend/src/services/api.js` 已新增 `fetchRecommend` 和 `submitRecommendFeedback`
- [x] `GlobalHeader.jsx` 中已新增 `/recommend` 导航，并复用 active 高亮
- [x] `npm run build` 通过
- [x] `/chat`、`/`、`/learning`、`/login`、`/register` 路由烟测均返回 200

## open-issues 补充
- 推荐页功能主链路已连通，前后端接口契约没有发现字段不一致；当前阻塞集中在 UI 验收证据和富文本安全边界。
- 项目整体尚未完成不影响本轮判断；这里的“未通过”仅针对 F4-1 推荐题目页当前 acceptance。

## 建议下一步
1. Coding Agent 先补 completed/skipped 状态 badge，确保完成后页面可直接看到“已完成”。
2. 移除或安全替换 `dangerouslySetInnerHTML`，不要把题库/LLM explanation 当可信 HTML 注入。
3. 补推荐页回归测试，至少覆盖状态 badge、反馈 payload、全部完成态和 explanation HTML 转义。
4. 修复后重新运行 `npm test`、`npm run build`，并复审 `/recommend` 主流程。

---

## F4-1 前端推荐题目页复审（修复后）

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `frontend/src/pages/RecommendPage/index.jsx:25` - `fetchRecommend().then(...).finally(...)` 仍没有 `catch` 或错误态；网络级 reject 时可能产生未处理 Promise rejection，并让用户看到空态而不是失败提示。该问题不影响当前 F4-1 核心验收，但建议后续补 `error` state，展示“推荐题目加载失败，请稍后重试”。
- [ ] 当前推荐页交互仍未纳入 `npm test` 自动化覆盖；建议后续补轻量页面状态测试，覆盖状态 badge、feedback payload、全部完成态和 explanation 安全渲染。

## 是否满足 acceptance
- [x] 是
- [x] `frontend/src/pages/RecommendPage/index.jsx` 存在
- [x] `/recommend` 路由已注册在 `ProtectedRoute > AppLayout` 下，路由烟测返回 200
- [x] 页面调用 `GET /api/recommend`，并通过 `requestJson` 自动携带 `Authorization: Bearer <token>`
- [x] loading skeleton 存在，且至少包含 1 张卡片骨架
- [x] 空态存在，并包含跳转 `/chat` 的引导按钮
- [x] 有数据时展示 topic、题干、选项列表和操作按钮
- [x] 点击选项会进入 selected 视觉状态
- [x] 查看/收起答案按钮会切换答案区域
- [x] `completed` 成功后会更新本地状态，并在详情区展示 “✔ 已完成” badge；左侧列表进入半透明 completed 态
- [x] `skipped` 成功后会更新本地状态，并在详情区展示 “→ 已跳过” badge
- [x] 所有卡片处理后展示“全部完成”区域，并提供“刷新推荐”按钮
- [x] `frontend/src/services/api.js` 已新增 `fetchRecommend` 和 `submitRecommendFeedback`
- [x] `frontend/src/App.jsx` 已在 `ProtectedRoute > AppLayout` 下注册 `/recommend`
- [x] `GlobalHeader.jsx` 中推荐导航已指向 `/recommend`，并复用 active 高亮
- [x] `npm run build` 通过
- [x] `/chat`、`/`、`/learning`、`/login`、`/register` 路由烟测均返回 200
- [x] 上轮 P1 安全问题已修复：`dangerouslySetInnerHTML` 已移除，`explanation` 作为 React 文本内容渲染

## open-issues 补充
- 无新增必须同步到 `project/open-issues.md` 的阻塞风险。
- 推荐页网络错误态和交互测试覆盖属于后续前端体验/质量增强项。

## 建议下一步
1. Leader 可收口 F4-1 前端推荐题目页。
2. 后续若进入体验优化，可优先补推荐页错误态与组件级回归测试。
