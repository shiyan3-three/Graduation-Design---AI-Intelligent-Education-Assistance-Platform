**核心开发原则**

1. **视觉 100% 还原**：禁止擅自修改我的颜色变量（如知性蓝 `#2563EB`、护眼青 `#0D9488`）、边距、字号和卡片布局。保留原型中的无 Icon 极简排版风格。
2. **数据驱动驱动视图**：不要在 JSX 里写死大段的文本。请把 HTML 里的业务数据（如“Weilin”的用户名、标日第 31 课的学习进度、“～でしょうか”的语法错题记录等）提取为分离的 Mock 数据对象或数组，通过 `map` 渲染。

**组件拆分与架构要求** 请按照以下层级将页面进行模块化拆分：

**1. 全局布局组件 (`src/components/Layout/AppLayout.tsx`)**

- 这是一个包裹页面的 Layout 容器，用于处理全局路由占位 `<Outlet />`。
- **Header 模块化**：必须将原型中的 `<header class="global-header">` 提取为独立的 `<GlobalHeader />` 组件，引入到 Layout 中，确保它在 `/chat`、`/analytics` 等所有路由页面顶部都存在且状态共享。

**2. 页面级组件 (`src/pages/Dashboard/index.tsx`)**

- 将主内容区 `<main class="main-container">` 作为仪表盘页面的主体。
- 内部进一步拆解为独立的纯展示组件：
  - `<MetricCard />`: 顶部三个核心指标卡片。
  - `<MistakeList />`: 左侧的薄弱点与错题列表。
  - `<RecommendTasks />`: 右侧靶向练习推荐模块。
  - `<Timeline />`: 右侧下方的学习记录时间线模块。

**状态管理与交互逻辑改造**

1. **Header 下拉菜单**：
   - 废弃原型中的原生 JS 做法。请在 `<GlobalHeader />` 中使用 React 的 `useState` 来管理菜单的 `isOpen` 状态。
   - 必须实现一个自定义 Hook（如 `useOnClickOutside`），确保用户点击菜单外部时能自动关闭下拉面板。
2. **深浅色主题切换 (Dark/Light Mode)**：
   - 提取为全局 Hook `useTheme`。
   - 将原型中的 `document.body.classList.toggle('light-mode')` 改造为 React 状态，并将其持久化存储到 `localStorage` 中，防止页面刷新后主题闪烁。

**样式处理方案**

- 请使用 **CSS Modules** (如 `Dashboard.module.css`) 或 **Tailwind CSS** 来接管原型中的样式。
- **重点保留**：原型中定义在 `:root` 和 `body.light-mode` 下的 CSS 变量机制，这是保证深浅色切换完美运行的核心，切勿破坏。

请输出完整的组件目录结构设计，并给出 `<GlobalHeader />` 和 `Dashboard/index.tsx` 的核心代码实现。