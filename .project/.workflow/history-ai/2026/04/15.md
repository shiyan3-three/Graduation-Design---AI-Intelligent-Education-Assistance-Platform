# 2026-04-15

## 当日关键进展

- 完成 F2-3 前端工具调用状态展示：提取 `message-element.js` 消息渲染模块，工具标签显示工具名 + 成功/失败状态；修复 `server.js` 静态资源路由导致的 `message-element.js` 404 问题。
- 完成 B3-2 `knowledge_tool` 接入：百度搜索 API（千帆 `web_search`）实现，从 `references` 提取 3-5 条结构化摘要；mock/fallback 支持"什么是"/"解释"类知识查询。
- 新增 `backend/env.py` 统一 `.env` 自动加载；新增 `backend/.env.example` 含 `BAIDU_SEARCH_API_KEY`。
- 前端 15 项测试全部通过；后端 15 项测试全部通过。
- 补充工具触发识别和 `tool_suggestion`/`tool_preference` 机制，使 AI 可以建议用户使用特定工具。

## 关键决策

- `knowledge_tool` 复用 B3-1 的 tool loop 结构，只新增工具注册和执行分发，不扩大改动范围。
- 为 API Key 复用 `backend/.env` 本地加载机制，不依赖操作系统级环境变量。
- mock/fallback 只对"什么是"/"解释"/"能"类知识查询触发 `knowledge_tool`。
- 消息渲染提取为 `message-element.js`，`app.js` 和 `sidebar-app.js` 共用一份工具标签逻辑。
- 工具触发采用"前缀/关键字匹配"识别，`tool_suggestion` 仅作为 `/api/chat` 响应中的推荐提示。

## 当前遗留问题

- `knowledge_tool` 搜索结果未做站点过滤，`snippet` 缺失时回退到整段 `content` 可达 3-4 KB，后续需截断或清洗。
- `sidebar-app.js` 和 `app.js` 各有重复的消息渲染逻辑，后续可合并。

## 给下一个 Agent 的交接

- B3-2 和 F2-3 均已完成，下一步应做 B3-3 `question_tool` 边界明确与实现。
- 前端两套页面入口的工具标签样式已同步。
- `backend/.env` 中需配置 `BAIDU_SEARCH_API_KEY` 才能使用真实知识搜索。
- 后端 22 项测试 + 前端 19 项测试均已通过。
