# Review 2026-04-17

## M4-2 MCP 迁移复审

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] Windows 沙箱下真实 `stdio` 子进程与部分 SQLite 写库测试仍需要提权环境；这不是本轮业务问题，但后续复审仍要保留这个验证前提
- [ ] 当前 MCP client 仍是“每次调用新建一个 `stdio` 连接”的最小实现，性能优化留待后续阶段

## 是否满足 acceptance
- [x] 是
- [x] `backend/mcp_server.py` 现已注册 `calculator_tool`、`knowledge_tool`、`question_tool`
- [x] 真实 `list_tools()` 返回 `["calculator_tool", "knowledge_tool", "question_tool"]`
- [x] `backend/services/llm.py` 中 `_build_knowledge_reply()` 已改为 `call_mcp_tool(...)` 优先、失败回退 direct call
- [x] `backend/services/llm.py` 中 `_build_question_reply()` 已改为 `call_mcp_tool(...)` 优先、失败回退 direct call
- [x] 三个工具的 MCP 失败回退逻辑仍存在，并在后端回归中被覆盖
- [x] 真实 `/api/chat` 验证：`什么是勾股定理` 走 `knowledge_tool`，返回知识摘要
- [x] 真实 `/api/chat` 验证：`给我出一道关于二次函数的题` 走 `question_tool`，返回结构化题目
- [x] 真实 `/api/chat` 验证：`计算 2 的 10 次方` 走 `calculator_tool`，返回 `1024`
- [x] 后端服务可正常启动，无新增阻塞性报错
- [x] 前端回归：`npm test -- --runInBand` 全部通过
- [x] 提权后端回归：`python -m unittest backend.tests.test_mcp_server backend.tests.test_sessions_api backend.tests.test_calculator_tool backend.tests.test_knowledge_tool backend.tests.test_llm_tool_loop backend.tests.test_chat_tools_api backend.tests.test_tool_triggering` 34/34 通过
- [x] 真实 MCP 调用：`call_mcp_tool('calculator_tool', {'expression': '2^10'})` 返回 `1024`

## open-issues 补充
- 本轮未新增阻塞性 open issue；沿用既有阶段性风险即可

## 建议下一步
1. Leader 可收口 M4-2
2. 按 `task.md` / `status.md` 进入下一任务，或由 Leader 决定后续 MCP 生命周期优化是否独立立项

## M4-1 修复后复审

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] Windows 沙箱下真实 `stdio` 子进程与部分 SQLite 写库测试仍需要提权环境；这不是本轮业务问题，但后续复审仍要保留这个验证前提
- [ ] 当前 MCP client 仍是“每次调用新建一个 `stdio` 连接”的最小实现，性能优化留待后续阶段

## 是否满足 acceptance
- [x] 是
- [x] `backend/mcp_server.py` 存在，并继续使用 `FastMCP("EduTools")` 注册 `calculator_tool`
- [x] `calculator_tool` 已作为唯一 MCP 工具注册；真实 `list_tools()` 返回 `["calculator_tool"]`
- [x] `python backend/mcp_server.py` 与 `python -m backend.mcp_server` 均不再报 `ModuleNotFoundError`，脚本模式与模块模式启动入口都可工作
- [x] `backend/services/llm.py` 中 `calculator_tool` 调用路径仍为 MCP 优先、失败回退 direct call
- [x] 真实 `/api/chat` 验证：`计算 2 的 10 次方` 返回 `1024`，且 `tools_used[0].tool_name == "calculator_tool"`
- [x] MCP 失败回退逻辑仍存在，并在后端回归中被覆盖
- [x] `knowledge_tool` 与 `question_tool` 真实 `/api/chat` 回归正常，未被本轮破坏
- [x] `backend/requirements.txt` 已包含 `mcp>=1.0.0`，本地安装版本为 `1.27.0`
- [x] 后端服务可正常启动，无新增阻塞性报错
- [x] 前端回归：`npm test -- --runInBand` 全部通过
- [x] 提权后端回归：`python -m unittest backend.tests.test_mcp_server backend.tests.test_sessions_api backend.tests.test_calculator_tool backend.tests.test_knowledge_tool backend.tests.test_llm_tool_loop backend.tests.test_chat_tools_api backend.tests.test_tool_triggering` 31/31 通过
- [x] 真实 MCP 调用：`call_mcp_tool('calculator_tool', {'expression': '2^10'})` 返回 `1024`

## open-issues 补充
- 本轮未新增阻塞性 open issue；沿用既有阶段性风险即可

## 建议下一步
1. Leader 可收口 M4-1
2. 进入 M4-2 时，再统一处理 `knowledge_tool` / `question_tool` 的 MCP 迁移与 client 生命周期优化

## M4-1 MCP Server 初审

## 结论
- [ ] 未通过

## 阻塞问题
- [ ] `backend/mcp_server.py:5` - `python backend/mcp_server.py` 会直接报 `ModuleNotFoundError: No module named 'backend'`，当前只能通过 `python -m backend.mcp_server` 启动，未满足 acceptance 中“脚本路径启动也能独立运行”的标准

## 非阻塞问题
- [ ] Windows 沙箱内真实 `stdio` 子进程验证仍需提权，沙箱下会出现 `WinError 5`；这与本轮 blocker 无关，但后续 review 仍需保留这条验证前提

## 是否满足 acceptance
- [ ] 否
- [x] `backend/mcp_server.py` 已存在，并使用 `FastMCP("EduTools")` 注册 `calculator_tool`
- [x] 真实 MCP 验证：`list_tools()` 仅返回 `calculator_tool`，`call_mcp_tool('calculator_tool', {'expression': '2^10'})` 返回 `1024`
- [x] `backend/services/llm.py` 中 calculator 路径已改为 MCP 优先、失败回退 direct call，并覆盖直达回复与 tool-call loop
- [x] 提权后端回归：`python -m unittest backend.tests.test_sessions_api backend.tests.test_calculator_tool backend.tests.test_knowledge_tool backend.tests.test_llm_tool_loop backend.tests.test_chat_tools_api backend.tests.test_tool_triggering` 30/30 通过
- [x] 真实后端 `/api/chat` 验证：`计算 2 的 10 次方` 返回 `1024` 且 `tools_used[0].tool_name == "calculator_tool"`；`knowledge_tool` 与 `question_tool` 回归正常
- [x] `backend/requirements.txt` 已追加 `mcp>=1.0.0`，本地安装版本为 `1.27.0`
- [x] 前端回归：`npm test -- --runInBand` 全部通过
- [ ] 但 `python backend/mcp_server.py` 当前不能独立运行，因此本轮不能判定通过

## open-issues 补充
- 当前最小 MCP client 每次调用新建一个 `stdio` 连接，性能优化仍留待后续阶段；这点与本轮未通过无关，但建议 Leader 继续保留为阶段性已知风险

## 建议下一步
1. 修复 `backend/mcp_server.py` 的脚本模式启动路径，让 `python backend/mcp_server.py` 与 `python -m backend.mcp_server` 都能工作
2. 修复后至少重跑三项验证：脚本路径启动、真实 `call_mcp_tool()`、真实 `/api/chat` 计算链路

## B3-3 连接修复后复审

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] Windows 沙箱内的 SQLite 临时库仍会触发 `disk I/O error` / 只读限制，所以后端写库验证仍建议沿用提权环境；这属于验证环境问题，不是本轮业务回归

## 是否满足 acceptance
- [x] 是
- [x] `backend/tools/question_tool.py` 和 `backend/tools/import_ceval.py` 已改为显式关闭 SQLite 连接，本轮未再复现 `ResourceWarning: unclosed database`
- [x] 提权后端用例：`python -W error::ResourceWarning -m unittest backend.tests.test_download_ceval backend.tests.test_question_tool backend.tests.test_chat_tools_api backend.tests.test_llm_tool_loop backend.tests.test_tool_triggering` 24/24 通过，说明连接泄漏告警已消失
- [x] 真实导入验证：`backend/tools/import_ceval.py` 仍可将 `backend/tools/ceval_data/*.csv` 导入运行库，结果为 1396 道题、7 个学科
- [x] 真实 HTTP 扩展题目验证：`二次函数`、`牛顿第二定律`、`光合作用` 命中 `question_tool` 且来自本地题库；`勾股定理` 走 `llm_generated` 兜底；返回仍包含结构化题面与答案
- [x] 老工具回归：`计算 2 的 10 次方` 继续走 `calculator_tool` 返回 `1024`；`什么是勾股定理` 继续走 `knowledge_tool`
- [x] 前端回归：`npm test -- --runInBand` 全部通过，工具标签与历史消息展示未受本轮修复影响

## open-issues 补充
- 目前 `database.get_connection()` 仍是裸连接接口，后续如果新增 SQLite 调用点继续写成 `with database.get_connection()`，仍有机会重犯同类问题；进入 MCP 阶段前可考虑补一条团队约定或轻量封装

## 建议下一步
1. Leader 可收口 B3-3 `question_tool`
2. 进入第 4 阶段前，可把“SQLite 连接使用约定”记入文档或 open-issues，避免后续新增调用点回归

## B3-3 question_tool 复审

## 结论
- [ ] 未通过

## 阻塞问题
- [ ] `backend/tools/question_tool.py:39` - `with database.get_connection() as connection` 不会自动关闭 SQLite 连接；`question_tool` 每次查本地题库都会泄漏连接，我在重跑后端用例时稳定看到了多次 `ResourceWarning: unclosed database`
- [ ] `backend/tools/import_ceval.py:57` - 导入脚本也使用了同样的连接管理写法，真实导入 7 个 CSV 后会遗留未关闭连接；在 Windows/SQLite 组合下有数据库文件锁和句柄泄漏风险

## 非阻塞问题
- [ ] 沙箱内 SQLite 与临时目录写入仍需提权验证；这属于验证环境限制，不是业务实现问题

## 是否满足 acceptance
- [ ] 否
- [x] `download_ceval.py`、`import_ceval.py`、`question_tool.py`、`llm.py`、前端标签映射和回归测试文件都已落地
- [x] 提权后端用例：`python -m unittest backend.tests.test_download_ceval backend.tests.test_question_tool backend.tests.test_chat_tools_api backend.tests.test_llm_tool_loop backend.tests.test_tool_triggering` 24/24 通过
- [x] 真实导入验证：现有 `backend/tools/ceval_data/*.csv` 可导入临时 SQLite，`question_bank` 共导入 1396 道题、覆盖 7 个学科
- [x] 真实 HTTP 扩展题目验证：`二次函数`、`牛顿第二定律`、`光合作用` 命中本地 `ceval`；`勾股定理` 走 `llm_generated` 兜底；返回结构都包含 `question/options/answer/source`
- [x] 真实 HTTP 回归：`计算 2 的 10 次方` 仍返回 `1024`，`什么是勾股定理` 仍触发 `knowledge_tool`
- [x] 前端回归：`npm test` 21/21 通过，`question_tool -> 出题` 标签映射未破坏已有工具标签展示
- [ ] 但当前 `question_tool` 与导入脚本都存在稳定复现的 SQLite 连接泄漏，不能判定本轮整体通过

## open-issues 补充
- `question_tool` 与导入脚本的 SQLite 连接生命周期需要统一收口；进入第 4 阶段 MCP Server 前最好先修掉，否则调用量上来后更容易放大为资源泄漏或 DB 锁问题

## 建议下一步
1. 将 `backend/tools/question_tool.py` 和 `backend/tools/import_ceval.py` 中的数据库连接改为显式关闭，例如 `with closing(database.get_connection()) as connection`
2. 修复后重跑同一组后端用例，并确认 `ResourceWarning: unclosed database` 消失
3. 保留本轮已经验证通过的多主题出题样例，作为回归集继续复测
