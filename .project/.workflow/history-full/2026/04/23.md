# 2026-04-23

## 任务：B-Admin 后端管理员模块

### 做了什么
- 核对 Leader 最新任务、验收标准、上下文、项目状态、roadmap 与系统模块设计方案，确认 B-Admin 与第 9 阶段管理员模块方向一致。
- 按 TDD 新增 `backend/tests/test_admin_api.py`，覆盖管理员接口的 6 个核心验收场景，并先验证 `/api/admin/*` 缺失导致 404 红灯。
- 在 `backend/database.py` 增加 `users.is_admin` 幂等迁移，并注入固定测试管理员 `id=9999`。
- 在 `backend/dependencies/auth.py` 增加管理员鉴权依赖，普通用户或不存在用户访问管理员接口返回 403。
- 新增 `backend/routers/admin.py`，实现用户列表、系统统计、删除用户三个管理员接口。
- 在 `backend/models.py` 增加管理员响应模型，并在 `backend/main.py` 注册 admin router。

### 改动文件
- `backend/database.py` - 增加 `is_admin` 字段、幂等迁移 `_run_migrations()`、`ensure_test_admin()`，并在初始化流程中执行。
- `backend/dependencies/auth.py` - 新增 `get_current_admin_user_id()`，基于当前 JWT 用户查询数据库管理员权限。
- `backend/models.py` - 新增 `AdminUserOut`、`AdminUsersResponseData`、`AdminStatsResponseData`。
- `backend/routers/admin.py` - 新增 `/api/admin/users`、`/api/admin/stats`、`DELETE /api/admin/users/{user_id}`。
- `backend/main.py` - 注册管理员路由。
- `backend/tests/test_admin_api.py` - 新增 6 条 B-Admin 回归测试。
- `.project/.workflow/history-full/2026/04/23.md` - 记录本轮完整工作日志。

### 关键决策
- roadmap 和系统模块设计写的是 `role=student/admin`，但当前任务与验收标准明确要求 `is_admin` 布尔字段；本轮按 `is_admin` 实现，避免引入超出 B-Admin 范围的权限组设计。
- 管理员用户使用任务要求的固定 `id=9999` 与 `dev-only-hash`，本轮不新增管理员注册或登录扩展。
- 删除用户依赖 SQLite 外键级联，保持已有 `ON DELETE CASCADE` 设计，不手写额外清理逻辑。

### 遇到的问题
- 沙箱内直接运行 SQLite 临时库测试出现 `disk I/O error`，改用提权测试命令后确认真实红灯与绿灯结果。
- 当前工作区不是 Git 仓库，无法使用 `git diff` 自查；改用定向测试、完整 discovery 和关键代码位置扫描确认范围。

### 当前结果
- `backend.tests.test_admin_api`：6/6 通过。
- `python -m unittest discover -s backend/tests`：96/96 通过。
- B-Admin 后端管理员模块已实现，等待人类验证和 Review Agent 复审。
---

## 任务：B-Admin-2 + F-Admin 题库 CRUD 后端 + 前端管理员面板

### 做了什么
- 核对 Leader 最新任务、验收标准、roadmap、系统模块设计方案与前端原型 `frontend/reflection/8.md`，确认本轮任务合理，且管理员入口必须仅管理员可见。
- 按 TDD 新增 `backend/tests/test_admin_questions.py`，覆盖登录返回 `is_admin`、管理员切换、题库列表过滤、题库新增/编辑/删除与缺字段 422。
- 扩展后端管理员模块，补齐 `/api/admin/users/{id}/admin` 与 `/api/admin/questions` CRUD。
- 扩展前端登录持久化与全局头部，确保 `edu_user.is_admin === true` 时才展示管理员入口。
- 新增 `/admin` 管理员页面，按原型实现数据总览、用户管理、题库管理三个页签，并提供题库内联新增、编辑、删除。
- 补充前端管理员访问辅助函数与测试，完成后端全量测试、前端测试、构建和路由烟测。

### 改动文件
- `backend/models.py` - 登录用户响应加入 `is_admin`，新增题库管理请求/响应模型。
- `backend/serializers.py` - 用户序列化补充 `is_admin` 字段。
- `backend/routers/auth.py` - 登录和当前用户查询读取 `is_admin`。
- `backend/routers/admin.py` - 新增管理员切换与题库 CRUD 接口。
- `backend/tests/test_admin_questions.py` - 新增 B-Admin-2 后端回归测试。
- `frontend/package.json` - 将管理员页面测试纳入 `npm test`。
- `frontend/src/admin-access.js` - 新增本地管理员身份读取工具。
- `frontend/src/services/api.js` - 新增管理员统计、用户、题库 API 客户端方法。
- `frontend/src/pages/AuthPage.jsx` - 登录后保存 `id` 与 `is_admin`。
- `frontend/src/components/Layout/AppLayout.jsx` - `/api/auth/me` 后同步最新用户信息到本地存储。
- `frontend/src/components/GlobalHeader/GlobalHeader.jsx` - 管理员入口按 `is_admin` 条件展示并跳转 `/admin`。
- `frontend/src/components/GlobalHeader/GlobalHeader.module.css` - 增加管理员入口样式。
- `frontend/src/App.jsx` - 注册 `/admin` 路由。
- `frontend/src/pages/AdminPage/index.jsx` - 新增管理员面板页面。
- `frontend/src/pages/AdminPage/AdminPage.module.css` - 新增管理员面板样式。
- `frontend/tests/admin-page.test.js` - 新增管理员访问与页面静态检查。
- `.project/.workflow/history-full/2026/04/23.md` - 追加本轮工作日志。

### 关键决策
- roadmap 和系统模块设计方案偏向管理员模块与题库管理，本轮按当前验收标准使用 `is_admin` 布尔字段，不额外引入 role/permission 体系。
- 题库后端完整支持 subject/topic/options 更新；前端按验收要求优先实现题目、选项、答案的内联编辑，避免扩展超出当前原型范围。
- 管理员页使用前端路由守卫加后端接口鉴权双保险；真实安全边界仍以后端 401/403 为准。

### 遇到的问题
- 初始红灯验证中，登录响应缺少 `is_admin`，管理员切换和题库接口均未实现，符合 TDD 预期。
- 当前历史日志文件已有内容在 PowerShell 输出中显示乱码；本轮只追加新日志，不修改已有记录，避免扩大影响。

### 当前结果
- `backend.tests.test_admin_questions`：8/8 通过。
- `backend.tests.test_admin_api`：6/6 通过。
- `backend.tests.test_auth_api`：8/8 通过。
- `python -m unittest discover -s backend/tests`：104/104 通过。
- `node tests/admin-page.test.js`：3/3 通过。
- `npm test`：通过。
- `npm run build`：通过。
- 前端路由烟测 `/`、`/chat`、`/learning`、`/recommend`、`/login`、`/admin` 均返回 200。

---

## 任务：最终收口 - 多管理员配置与 AdminPage 网络兜底

### 做了什么
- 根据 review 非阻塞风险和最终演示需求，将管理员配置从固定测试账号补充为 `.env` 多用户名列表。
- 新增后端回归测试，验证 `ADMIN_USERNAMES=admin1, teacher_admin, missing_admin` 只会提升已存在用户，不会自动创建缺失用户。
- 实现 `ADMIN_USERNAMES` 解析逻辑，并兼容旧名 `ADMIN_USERNAME`；启动初始化时将配置中的已存在用户提升为管理员。
- 将本地 `backend/.env` 配置为 `ADMIN_USERNAMES=admin1`，并更新 `backend/.env.example` 说明多管理员配置方式。
- 新增前端管理员请求兜底 helper，AdminPage 所有加载、增删改请求遇到网络 reject 时显示中文错误提示。
- 对运行库 `project-scipt/runtime/backend_runtime.db` 执行初始化迁移，确认 `admin1` 已提升为管理员。

### 改动文件
- `backend/database.py` - 新增多管理员用户名解析与提升逻辑，并在 `init_database()` 中执行。
- `backend/tests/test_admin_api.py` - 新增多管理员配置回归测试。
- `backend/.env.example` - 增加 `ADMIN_USERNAMES=admin1` 示例和说明。
- `backend/.env` - 本地配置 `ADMIN_USERNAMES=admin1`。
- `frontend/src/pages/AdminPage/admin-request.js` - 新增管理员请求安全包装函数。
- `frontend/src/pages/AdminPage/index.jsx` - 管理员页请求统一走网络异常兜底。
- `frontend/tests/admin-page.test.js` - 新增网络 reject 兜底测试。
- `.project/.workflow/history-full/2026/04/23.md` - 追加本轮最终收口日志。

### 关键决策
- `.env` 使用 `ADMIN_USERNAMES=admin1,teacher_admin` 这种逗号分隔列表，适配多个管理员；`ADMIN_USERNAME` 作为兼容别名保留。
- 配置只提升已存在账号，不自动创建新账号，避免生成没有可用密码的管理员。
- 保留 `id=9999` 固定测试管理员，避免破坏现有后端测试；真实演示入口改走 `admin1`。

### 遇到的问题
- 对运行库直接初始化时在默认沙箱下出现 SQLite `disk I/O error`，按既有处理方式提权重跑后成功。

### 当前结果
- `backend.tests.test_admin_api.AdminApiTests.test_init_database_promotes_configured_admin_usernames`：通过。
- `backend.tests.test_admin_api`：7/7 通过。
- `python -m unittest discover -s backend/tests`：105/105 通过。
- `node tests/admin-page.test.js`：4/4 通过。
- `npm test`：通过。
- `npm run build`：通过。
- 运行库验证：`has_is_admin=True`，`admin1={'username': 'admin1', 'is_admin': 1}`。
