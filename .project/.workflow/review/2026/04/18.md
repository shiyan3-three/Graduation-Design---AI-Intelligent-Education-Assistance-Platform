# Review 2026-04-18

## FR-1 前端 React 重构复审

## 结论
- [ ] 未通过

## 阻塞问题
- [ ] `frontend/src/components/InputBarReact.jsx:31` - 发送中/切换会话中未真正禁用输入提交。`textarea` 没有绑定 `disabled`，`frontend/src/components/ChatPanelReact.jsx` 的 `handleKeyDown()` 在禁用态仍会直接 `onSubmit()`，而 `frontend/src/chat-state.js` 的 `startSubmission()` 也没有对 `state.isSending` 做保护。我用命令行复核了这三层：当前即使 `isSending=true`，状态层仍会生成新的 `requestBody`。这与 acceptance 中“发送中状态（loading/禁用）”不符，也存在重复提交和串会话风险。

## 非阻塞问题
- [ ] `frontend/src/components/MessageBubbleReact.jsx:44` - tool suggestion 文案直接读取 `message.toolSuggestion.label`，但当前 `frontend/src/chat-state.js` 只保留 `tool_name` 和 `recommended_prompt`；我用真实 `5173/api/chat` 验证了 bare topic `勾股定理`，后端返回的 `tool_suggestion` 也确实没有 `label` 字段，所以页面会出现“当前问题更适合使用 来获取结果”的空白文案。按钮仍可用，但这是已有功能的显示回归。
- [ ] 本轮人类反馈已确认 `question_tool` 本地题库命中、MCP 子进程环境一致、前后端启动脚本可正常启动，但 FR-1 自身的浏览器专项验收还缺一轮：React 页面视觉质量、无明显 console 报错、侧栏切换/新建会话在新 UI 下是否顺畅，仍建议修复阻塞项后再补一轮人类走查。

## 是否满足 acceptance
- [ ] 否
- [x] `frontend/` 已是 Vite + React 工程，`package.json` 包含 react / react-dom / vite / `@vitejs/plugin-react`
- [x] `npm test` 全绿，`npm run build` 通过
- [x] Vite 开发服务已可通过 `http://127.0.0.1:5173/` 正常返回首页 HTML
- [x] 真实代理验证：`GET http://127.0.0.1:5173/api/sessions` 返回 `200`
- [x] 真实代理验证：`POST http://127.0.0.1:5173/api/chat` 可成功命中后端；你提供的人类反馈也确认了 `question_tool -> source=ceval`
- [x] 前后端启动脚本当前可正常启动并访问
- [x] 后端 API 契约本轮未被前端重构破坏，当前前端仍对接现有 `POST /api/chat`、`GET /api/sessions`、`GET /api/sessions/{id}`
- [ ] 但“发送中状态（loading/禁用）”当前未满足，不能判定整体通过

## open-issues 补充
- 当前前端测试仍以纯逻辑和旧 DOM 工具为主，React 组件交互的自动化覆盖较薄；FR-1 收口前建议至少补一条针对输入禁用/工具建议文案的组件级回归。

## 建议下一步
1. 修复输入区在 `state.isSending` / `state.isLoadingSessionDetail` 时的真实禁用：至少要禁用 `textarea`，并在提交入口或状态层增加二次保护，避免 Enter 重复发请求。
2. 修复 React 版 tool suggestion 文案映射：要么在 `normalizeToolSuggestion()` 中补 `label`，要么组件内根据 `tool_name` 现算标签。
3. 修复后重新做一轮 FR-1 浏览器专项验证：消息发送、侧栏切换、新建会话、工具标签、console。

---

## FR-1 修复后复审（输入禁用 + tool suggestion）

## 结论
- [ ] 未通过（待补人类浏览器验证）

## 阻塞问题
- [ ] 命令行侧未再发现新的代码阻塞问题；上轮两个问题已修复。但 FR-1 仍缺真实浏览器验收，当前无法仅凭命令行确认“对话主页面正常显示”“工具标签正确渲染”“侧栏切换/新建会话顺畅”“无明显 console 报错”“页面视觉质量达到专业水准”这几条前端 acceptance，因此本轮先不判定最终通过。

## 非阻塞问题
- [ ] `frontend/tests/` 目前仍以状态层和旧 DOM 工具回归为主，React 组件层针对“发送中禁用”和“tool suggestion 文案”没有新增自动化测试；本轮虽已靠命令行针对性复核修复结果，但后续仍建议补一条轻量组件级回归。

## 是否满足 acceptance
- [ ] 否（待补 GUI 验收）
- [x] `frontend/` 仍是 Vite + React 工程，`package.json` 包含 react / vite 依赖
- [x] `npm test` 全绿，`npm run build` 通过
- [x] `http://127.0.0.1:5173/` 仍可正常返回页面
- [x] 真实代理链路仍可用：`POST http://127.0.0.1:5173/api/chat` 对 bare topic `勾股定理` 返回 `tool_suggestion`
- [x] 代码复核 + 命令行复核显示：`textarea` 已绑定 `disabled`，`handleKeyDown()` 已在禁用态短路，`startSubmission()` 在 `isSending` / `isLoadingSessionDetail` 时返回 `requestBody = null`
- [x] 代码复核显示：React 版 tool suggestion 文案已改为 `getToolLabel(message.toolSuggestion.tool_name)`
- [ ] 但上述 GUI acceptance 仍需人类浏览器补验后才能收口

## open-issues 补充
- 本轮未新增项目级 open issue；维持“React 组件交互自动化覆盖偏薄”的已知风险判断即可。

## 建议下一步
1. 请人类在浏览器补验 4 个点：发送中连续按 Enter 不会重复提交；输入 `勾股定理` 时建议卡显示“知识查询”；点击“使用工具”后能正常触发推荐问题；侧栏切换和新建会话无明显异常、console 无明显报错。
2. 若上述浏览器检查全部通过，可据此再做一次最终复审收口 FR-1。

---

## FR-1 最终复审（补充人类浏览器验证）

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] `frontend/tests/` 目前仍以状态层和旧 DOM 工具回归为主，React 组件层针对“发送中禁用”和“tool suggestion 文案”的自动化覆盖偏薄；本轮虽已通过命令行复核和人类浏览器验证确认行为正确，后续仍建议补一条轻量组件级回归。

## 是否满足 acceptance
- [x] 是
- [x] `frontend/` 已是 Vite + React 工程，`package.json` 包含 react 和 vite 依赖
- [x] `npm run dev` 可正常启动开发服务器，`http://127.0.0.1:5173/` 可访问
- [x] 对话主页面可正常显示，包含消息列表、输入框、发送按钮
- [x] 发送消息后，后端可通过 Vite 代理正确响应
- [x] 工具调用标签正确渲染；bare topic 的 tool suggestion 文案也已正确显示工具名称
- [x] 侧栏显示会话列表，点击可切换会话并加载历史消息
- [x] 可新建对话
- [x] 页面视觉质量经人类浏览器走查未见明显问题，可满足演示要求
- [x] 后端 API 未被本轮前端重构修改
- [x] 人类补充验证确认浏览器 Console 无明显报错

## open-issues 补充
- 本轮未新增项目级 open issue；维持“React 组件交互自动化覆盖偏薄”的已知风险判断即可。

## 建议下一步
1. Leader 可收口 FR-1。
2. 进入下一任务前，可视团队节奏决定是否顺手补一条 React 组件级回归测试，但这不影响本轮通过。
