# 2026-04-22

## 任务：B2-增强 后端 Chat 流式输出（SSE）

### 做了什么
- 核对 Leader 任务与 roadmap、系统模块设计方案，确认第 8 阶段流式增强在 F4-1 收口后执行合理。
- 在 `backend/tests/test_chat_stream.py` 新增 SSE 接口回归测试，覆盖无 Token、空内容、mock 流式 delta/done、助手消息落库、直接工具调用流式返回。
- 在 `backend/services/llm.py` 新增 `generate_assistant_reply_stream`，支持直接工具结果整块输出、mock 文本分块输出、OpenAI-compatible SSE 解析与 provider failover。
- 在 `backend/routers/chat.py` 新增 `POST /api/chat/stream`，复用现有会话/消息持久化 helper，按 SSE `data: <json>` 输出 delta/error/done 事件。
- 运行新增定向测试与完整后端 discovery，确认现有 chat、sessions、analytics、recommend、auth、MCP 工具链路无回归。

### 改动文件
- `backend/routers/chat.py` - 新增 `/api/chat/stream`，在流完成后写入 assistant 消息并发送 `done` 事件。
- `backend/services/llm.py` - 新增流式 LLM 生成器、mock 分块输出、OpenAI-compatible SSE chunk 解析和 tool_call 参数缓冲。
- `backend/tests/test_chat_stream.py` - 新增 B2-增强 SSE 端点验收测试。
- `.project/.workflow/history-full/2026/04/22.md` - 记录本轮完整工作日志。

### 关键决策
- `/api/chat` 保持不变，`/api/chat/stream` 作为并行增强端点，避免破坏现有前端和后端测试。
- `done` 事件由路由层在 assistant 消息真实落库后发送，确保 `message_id` 是数据库中的真实 ID。
- 直接工具调用结果不拆 token，按整块 `delta` 输出；普通 mock 回复按 8 字分块，便于在无真实 LLM key 时验证流式效果。
- OpenAI-compatible 流式工具调用先缓冲完整 `tool_calls.function.arguments`，再执行工具并继续下一轮模型调用。

### 遇到的问题
- 首次在沙箱内运行 SQLite 临时库测试时出现 `disk I/O error`，改用提权测试命令后确认真正红灯为 `/api/chat/stream` 未实现。
- FastAPI 会把 `JSONResponse | StreamingResponse` 联合返回注解误解析为响应模型，已去掉该端点联合返回注解。
- 首次红灯失败遗留了 `project-scipt/runtime/test-chat-stream` 临时测试目录，已确认路径在项目工作区内后清理。

### 当前结果
- `backend.tests.test_chat_stream`：4/4 通过。
- `python -m unittest discover -s backend/tests`：86/86 通过。
- B2-增强后端 SSE 端点已完成，等待人类验证和 Review Agent 复审。

---

## 任务：F2-增强 前端流式消费 SSE 与 UX 优化

### 做了什么
- 核对 Leader 任务与 roadmap、系统模块设计方案，确认 `POST /api/chat/stream` 后端已就绪，SSE 协议完整。
- 修复项目内全体 JS 文件中系统性的 `? ?` / `? .` 操作符腐化（波及 `chat-state.js` 全部现有函数、`useChatAppReact.js` 现有 loadSessions/loadSessionDetail），替换为等价且无歧义的 `||` / `&&` 链式访问写法，保持语义不变。
- 在 `frontend/src/services/api.js` 新增 `streamChatRequest(body, onDelta, onDone, onError)`：手动注入 Bearer、fetch ReadableStream 逐行解析 SSE、401 自动跳 login、非 200 解析 JSON error、流读取异常调 onError。
- 在 `frontend/src/chat-state.js` 新增 4 个流式状态函数：`startStreamingAssistant`（同时 de-pending 用户消息，追加 streaming 气泡）、`appendStreamingDelta`、`finalizeStreamingAssistant`（设真实 id/sessionId、streaming→false）、`failStreamingAssistant`（移除 streaming 尾部气泡）。
- 在 `frontend/src/hooks/useChatAppReact.js` 将 `submitMessage` 改为 SSE 路径，首个 delta 到达时懒启动 `startStreamingAssistant`，done 时调 `finalizeStreamingAssistant` + 静默刷新侧栏，error/catch 时调 `failStreamingAssistant`。
- 在 `frontend/src/components/ChatPanelReact.jsx` 将 loading dots 条件改为 `isSending && !messages.some(m => m.streaming)`，首 delta 后自动切换为流式文字。
- 在 `frontend/src/components/MessageBubbleReact.jsx` 为 `streaming: true` 的消息 article 追加 `is-streaming` class。
- 在 `frontend/src/styles/app.css` 新增 `.message-bubble.is-streaming .message-bubble__content::after` 闪烁光标（`@keyframes cursorBlink`，1s step-end infinite）。

### 改动文件
- `frontend/src/services/api.js` - 新增 `streamChatRequest`；修复腐化的 `? .` 操作符
- `frontend/src/chat-state.js` - 新增 4 个流式状态函数；修复全部腐化的 `? ?` / `? .` 操作符
- `frontend/src/hooks/useChatAppReact.js` - `submitMessage` 改 SSE 路径；修复腐化操作符；更新 import
- `frontend/src/components/ChatPanelReact.jsx` - loading dots 条件更新
- `frontend/src/components/MessageBubbleReact.jsx` - 追加 `is-streaming` class
- `frontend/src/styles/app.css` - 新增流式光标 CSS

### 关键决策
- `startStreamingAssistant` 在首个 delta 到达时懒启动（而非 HTTP 响应头到达时），保证 loading dots 在 LLM 开始输出前持续可见
- `??` / `?.` 统一替换为 `||` / `&&` 链式访问，彻底消除工具链空格插入腐化风险
- `is-streaming` 通过 CSS `::after` pseudo-element 实现光标，不修改 JSX 内容结构

### 遇到的问题
- 工具调用的 `??` 和 `?.` 操作符在写入文件时被系统性插入空格（`? ?` / `? .`），导致 Rollup 构建失败，需全文件扫描替换为等价写法

### 当前结果
- `npm run build`：✓ 320 modules transformed，1.77s，exit 0
- F2-增强前端 SSE 实现完成，等待人类浏览器验证和 Review Agent 复审

---

## 任务：F2-增强 前端流式消费 SSE 与滚动体验修复

### 做了什么
- 阅读 F2-增强任务、验收标准、上下文和当日日志，确认上一轮前端已接入 `/api/chat/stream`，但存在流式体感与滚动体验问题。
- 定位流式体感问题：当前前端只按后端 `delta` 事件粒度更新；一旦后端、代理或工具调用返回大块 delta，浏览器仍会一次性显示大段内容。
- 定位滚动问题：`ChatPanelReact` 只在距离底部 120px 内才自动滚动；长回复渲染后会把页面瞬间撑高，导致系统误判用户“不在底部”，后续 token 不再跟随。
- 新增 `frontend/src/chat-scroll.js`，把“何时应自动贴底”的判断抽成纯函数，并在流式中、发送中、旧会话详情加载完成后强制贴底。
- 更新 `streamChatRequest`，兼容对象回调和旧的分参数回调，并对大块 delta 做客户端打字机拆分，保证前端可见逐步出现效果。
- 调整聊天区底部 padding，降低浮动输入框遮挡最新回复的概率。

### 改动文件
- `frontend/src/services/api.js` - `streamChatRequest` 支持大块 delta 客户端打字机拆分、对象回调签名和 response.body 缺失错误处理。
- `frontend/src/chat-scroll.js` - 新增滚动策略与贴底工具函数。
- `frontend/src/components/ChatPanelReact.jsx` - 改用滚动策略 helper，在流式输出和旧会话加载完成时稳定滚到底部。
- `frontend/src/hooks/useChatAppReact.js` - 改用对象形式调用 `streamChatRequest`，与 Leader 指令保持一致。
- `frontend/src/styles/app.css` - 增加聊天流底部 padding，避免最新回复被浮动输入框压住。
- `frontend/tests/chat-scroll.test.js` - 覆盖流式消息、旧会话加载完成和用户主动阅读旧消息时的滚动策略。
- `frontend/tests/stream-chat-request.test.js` - 覆盖单个大 delta 被拆成多次前端更新。
- `frontend/package.json` - 将新增前端测试加入 `npm test`。

### 关键决策
- 不依赖后端一定按 token 细粒度输出；前端额外做轻量 typewriter 分片，保证用户可见的逐步出现效果。
- 流式输出和旧会话加载完成属于“系统应该带用户去看最新内容”的场景，优先自动贴底；普通空闲状态下保留用户阅读旧消息的位置。
- 未修改 backend 文件，避免把 F2-增强的问题扩大到 B2-增强后端任务范围。

### 遇到的问题
- 前端现有测试环境没有 jsdom，无法直接测真实 DOM 滚动；因此将滚动触发条件抽成纯函数做回归测试，实际滚动动作通过构建和人工浏览器验证补充。

### 当前结果
- `npm test`：全部通过。
- `npm run build`：✓ 321 modules transformed，1.52s，exit 0。
- 已修复前端流式体感与滚动策略问题，仍建议人类在浏览器中补充验证长会话滚动和旧会话打开位置。

---

## 任务：F2-增强 Review 阻塞问题修复（failStreamingAssistant 错误状态）

### 做了什么
- 阅读 Review 反馈，确认阻塞点为 `failStreamingAssistant()` 在网络断开或 SSE error 后只设置全局错误，不会让任何消息进入 `failed: true` 状态。
- 在 `frontend/tests/chat-state.test.js` 新增两条回归测试，覆盖首个 delta 前失败和流式输出中失败两条路径。
- 修改 `frontend/src/chat-state.js`，让 `failStreamingAssistant()` 在移除 streaming assistant 尾部消息后，将最近一条用户消息标记为 `failed: true` 且 `pending: false`。

### 改动文件
- `frontend/src/chat-state.js` - 修复 `failStreamingAssistant()` 的失败状态收尾，新增最近用户消息失败标记 helper。
- `frontend/tests/chat-state.test.js` - 新增流式失败路径回归测试。
- `.project/.workflow/history-full/2026/04/22.md` - 记录本轮 Review 修复日志。

### 关键决策
- 保持原设计中“失败时移除 streaming assistant 占位气泡”的行为，只把触发该次请求的用户消息标记为 failed，复用现有 UI 的“发送失败”状态展示。
- 不修改后端和 SSE 消费逻辑，避免扩大本轮 Review blocker 范围。

### 遇到的问题
- 修复前新增测试准确复现 Review 结论：失败后没有任何 `failed: true` 消息。

### 当前结果
- `node tests/chat-state.test.js`：8/8 通过。
- `npm test`：全部通过。
- `npm run build`：✓ 321 modules transformed，1.57s，exit 0。

---

## 任务：B-Analytics-增强 后端学习汇总统计接口

### 做了什么
- 核对 Leader 任务与 roadmap、系统模块设计方案，确认本轮是为 Dashboard 真实数据补齐后端汇总指标，范围合理。
- 按 TDD 在 `backend/tests/test_analytics_api.py` 先补充 summary 接口测试，覆盖无 Token、空数据、有数据、streak 过期边界。
- 在 `backend/routers/analytics.py` 新增 `GET /api/analytics/summary`，基于现有 `sessions` 与 `recommendation_items` 计算学习天数、连续学习天数和推荐完成率。
- 在 `backend/models.py` 新增 `AnalyticsSummaryResponseData` 响应模型。

### 改动文件
- `backend/routers/analytics.py` - 新增 summary 端点及 `_fetch_total_study_days`、`_compute_streak`、`_compute_recommend_rate` 三个计算 helper。
- `backend/models.py` - 新增 `AnalyticsSummaryResponseData`。
- `backend/tests/test_analytics_api.py` - 新增 4 条 summary 回归测试和测试数据 helper。
- `.project/.workflow/history-full/2026/04/22.md` - 记录本轮完整工作日志。

### 关键决策
- `streak_days` 以最近学习日期是否为今天或昨天作为活跃判断；若最近学习已在两天前，则直接返回 0，符合 acceptance。
- `recommend_complete_rate` 只统计 `completed` 与 `skipped`，忽略 `pending`；无 completed/skipped 记录时返回 `null`。
- 不改前端、不改数据库 schema、不改已有 tags/report 端点，避免扩大 B-Analytics-增强范围。

### 遇到的问题
- 新增测试红灯阶段 4 个 summary 用例均返回 404，确认问题是端点尚未实现；补齐端点后转绿。
- 当前工作区不是 Git 仓库，无法用 `git diff` 查看变更；已通过定向测试和完整后端测试验证改动范围与行为。

### 当前结果
- `backend.tests.test_analytics_api`：9/9 通过。
- `python -m unittest discover -s backend/tests`：90/90 通过。

---

## 任务：首页 Dashboard 数据联调问题修复

### 做了什么
- 排查首页累计学习天数、推荐完成率、连续打卡天数显示为 `—/--` 的原因，确认运行中的后端对 `/api/analytics/summary` 返回 404。
- 重启后端服务后，无 Token 请求 `/api/analytics/summary` 返回 401，确认最新 summary 路由已加载。
- 排查首页“靶向推荐任务”空白卡片，确认前端读取了不存在的 `item.question` / `item.topic` 字段。
- 新增首页推荐预览映射 helper，并让首页从 `question_payload.question` 与 `recommended_topic` 渲染推荐卡片。

### 改动文件
- `frontend/src/home-dashboard.js` - 新增 `getRecommendPreview()`，统一把后端推荐项映射成首页卡片标题和副标题。
- `frontend/src/pages/HomePage/index.jsx` - 修复靶向推荐任务字段映射。
- `frontend/tests/home-dashboard.test.js` - 新增推荐任务字段映射回归测试。
- `frontend/package.json` - 将首页 Dashboard 回归测试加入 `npm test`。
- `.project/.workflow/history-full/2026/04/22.md` - 记录本轮诊断与修复。

### 关键决策
- summary 三项指标不做前端兜底假数据修复，因为根因是后端旧进程未加载新路由；前端继续使用真实接口数据。
- 推荐任务只修字段映射，不改变 `/api/recommend` 的后端契约。

### 遇到的问题
- 运行中的后端进程早于 summary 端点实现启动，导致浏览器请求持续 404；通过重启后端解决。

### 当前结果
- `node tests/home-dashboard.test.js`：2/2 通过。
- `npm test`：全部通过。
- `npm run build`：通过，322 modules transformed。
- `/api/analytics/summary` 无 Token 请求已从 404 变为 401，说明路由已加载。

---

## 任务：首页靶向推荐卡片长题目布局修复

### 做了什么
- 排查首页靶向推荐任务中长题目挤压“去练习”按钮的问题，确认根因是卡片 flex 布局缺少文字区最小宽度、换行和按钮防收缩约束。
- 在 `frontend/tests/home-dashboard.test.js` 增加 CSS 回归断言，覆盖长标题不挤压按钮的关键样式。
- 调整首页列表卡片样式，让题目区域可换行、按钮固定不换行，并在窄屏下改为上下堆叠。

### 改动文件
- `frontend/src/pages/HomePage/HomePage.module.css` - 为 `.listItem` 增加 gap 和顶部对齐；为 `.itemInfo` 增加 `min-width: 0`；为 `.itemTitle` 增加换行策略；为列表内 `.primaryBtn` 增加不换行和固定收缩策略；补充移动端堆叠规则。
- `frontend/tests/home-dashboard.test.js` - 新增长题目布局回归测试。
- `.project/.workflow/history-full/2026/04/22.md` - 记录本轮 UI 修复。

### 关键决策
- 使用 flex gap 和 `white-space: nowrap` 保证按钮不被长文本挤压，不截断题目内容。
- 窄屏下让卡片内容与按钮上下排列，避免移动端横向拥挤。

### 遇到的问题
- 该问题主要是视觉布局问题，无法在现有测试环境中真实测量 DOM 宽度，因此用 CSS 关键规则断言配合构建验证。

### 当前结果
- `node tests/home-dashboard.test.js`：3/3 通过。
- `npm test`：全部通过。
- `npm run build`：通过，322 modules transformed。
