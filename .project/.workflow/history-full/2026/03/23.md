# 2026-03-23

## 任务：数据库设计初稿

### 做了什么
- 读取 `current/task.md`、`acceptance.md`、`context.md`、`project/status.md`、`project/decisions.md`、`project/open-issues.md`、最近 `history-ai/`，确认本轮只做数据库设计文档
- 读取 `.project/系统模块设计方案.md`，提取 B1 用户管理、B2 智能对话、B4 学习分析、B5 题目推荐相关的数据结构要求
- 新增 `.project/database-design.md`，输出 SQLite 目标下的数据库设计初稿

### 改动文件
- `.project/database-design.md` - 新增数据库设计草案，包含阶段划分、ER 关系、字段定义、DDL 草案、索引建议和 API 影响说明
- `.project/.workflow/history-full/2026/03/23.md` - 记录本轮完整执行日志

### 关键决策
- 主键统一使用 `INTEGER PRIMARY KEY`，不使用 UUID：更贴合 SQLite 单机单库场景，简化联调与实现
- `messages.tools_used` 先使用 JSON 字符串存储，不在当前阶段拆独立工具调用表：先保证 MVP 结构清晰，避免过早设计复杂化
- 推荐模块后续预留表采用 `recommendation_items` 单表方案：一题一行记录题目与反馈状态，先满足推荐与反馈闭环
- `messages` 增加 `sequence_no`、`sessions` 增加 `updated_at`：保证消息顺序稳定，会话列表可按最近活跃排序

### 遇到的问题
- 项目工作流目录实际位于 `.project/.workflow/`，与口头约定的 `.workflow/` 不一致；本轮按真实路径读取并执行
- 部分中文 Markdown 用 PowerShell 默认编码读取时出现乱码；改用 UTF-8 重新读取后恢复正常

### 当前结果
- 数据库设计初稿已形成，产出路径为 `.project/database-design.md`
- 当前文档已覆盖验收要求中的核心表、关系、阶段划分和后续 API 设计输入
- 尚未经过 Review Agent 审查，也未进入后端实现阶段

## 任务：API 设计初稿

### 做了什么
- 重新读取 `current/task.md`、`acceptance.md`、`context.md`、`project/status.md`、`project/decisions.md`、`project/open-issues.md`、最近 `history-ai/` 和最新 `review/`，确认 D-2 为当前唯一任务
- 读取 `.project/database-design.md` 与 `.project/系统模块设计方案.md`，将数据库字段、阶段划分和模块接口需求对齐
- 新增 `.project/api-design.md`，输出 API 统一约定、阶段划分，以及 B1、B2、B4、B5 四个模块的接口契约

### 改动文件
- `.project/api-design.md` - 新增 API 设计初稿，覆盖统一响应格式、鉴权规则、接口字段、状态码和阶段标注
- `.project/.workflow/history-full/2026/03/23.md` - 追加记录 D-2 执行日志

### 关键决策
- 统一成功/失败响应结构：减少后续前后端联调时的格式分歧
- 除 `password` 外，接口字段尽量复用数据库字段名：保证 API 契约与数据模型一致
- `GET /api/sessions`、`GET /api/sessions/{id}`、`POST /api/chat` 标记为第一阶段必须，其余接口标记为后续阶段预留：与项目推进顺序保持一致
- `tools_used` 和 `question_payload` 在 API 层输出结构化 JSON，在数据库层序列化存储：兼顾前端易用性和 SQLite 落库实现

### 遇到的问题
- `task.md` 中的“4 组接口”与 `context.md` 中列出的具体端点范围存在抽象层级差异；本轮按 `context.md` 列出的完整端点集合落文档，并保持仍只在 B1、B2、B4、B5 范围内
- 登录接口需要兼顾数据库字段对齐和安全性；因此接口输入采用 `password`，服务端内部映射到 `password_hash`

### 当前结果
- API 设计初稿已形成，产出路径为 `.project/api-design.md`
- 当前文档已覆盖验收要求中的 4 组核心接口，并补齐同模块相关接口
- 尚未经过 Review Agent 审查，也未进入后端实现阶段

## 任务：B2-1 后端最简 `/api/chat` 接口

### 做了什么
- 读取 `current/task.md`、`acceptance.md`、`context.md`、`project/status.md`、`project/decisions.md`、`project/open-issues.md`、最近 `history-ai/` 和最新 `review/`，确认当前任务为 B2-1
- 新建 `backend/` 后端骨架，实现 `main.py`、`database.py`、`models.py`、`routers/chat.py`、`services/llm.py`、`requirements.txt`、`README.md`
- 按 `.project/database-design.md` 初始化 SQLite 表结构，并确保每次连接执行 `PRAGMA foreign_keys = ON`
- 实现 `POST /api/chat`：支持 `session_id` 为空时新建会话，支持传入已有 `session_id` 时追加消息
- 实现 `messages.sequence_no` 应用层递增、`sessions.updated_at` 同步刷新、硬编码测试用户 `user_id = 1`
- 接入原生 HTTP 大模型调用封装；无环境变量或调用失败时自动降级到 Mock 回复，保证当前阶段主链路可运行
- 安装 FastAPI、Uvicorn、httpx、Pydantic 依赖，并完成启动验证与接口联调验证

### 改动文件
- `backend/main.py` - FastAPI 入口、lifespan 初始化、统一异常响应
- `backend/database.py` - SQLite 连接管理、建表脚本、测试用户初始化
- `backend/models.py` - `/api/chat` 请求与响应模型、统一成功/失败响应格式
- `backend/routers/chat.py` - `POST /api/chat` 路由与 session/message 基础读写逻辑
- `backend/services/llm.py` - 原生 HTTP 模型调用封装与 Mock 降级策略
- `backend/requirements.txt` - 后端依赖清单
- `backend/README.md` - 启动、环境变量和请求示例说明
- `.project/.workflow/history-full/2026/03/23.md` - 追加本轮完整执行日志

### 关键决策
- 当前阶段采用“真实模型调用 + Mock 降级”双路径：有环境变量就走真实 API，没有就自动返回演示回复，避免主链路被外部依赖卡死
- 严格沿用数据库设计中的 DDL，避免后端实现和文档契约再次漂移
- `POST /api/chat` 先落库用户消息，再生成助手回复，再落库助手消息：保证 `id`、`sequence_no` 和响应字段稳定可返回
- 统一把 FastAPI 默认请求校验错误转换为 400，并保持统一响应格式

### 遇到的问题
- `apply_patch` 在当前 Windows 沙箱中持续失败，无法创建文件；本轮改用 PowerShell 直接写文件继续推进
- 提权环境中的 `python` 指向 WindowsApps 占位符，导致依赖安装命令看似执行但实际未跑；改用真实解释器 `E:\Python\python.exe` 后安装成功
- 直接实例化 `TestClient(app)` 不会触发 lifespan；改为 `with TestClient(app)` 后数据库初始化正常执行

### 当前结果
- `uvicorn` 可成功启动 FastAPI 服务
- `POST /api/chat` 已可正常返回统一 JSON 结构，包含 `session`、`user_message`、`assistant_message`
- 新会话创建、已有会话追加、`sequence_no` 递增、`updated_at` 刷新、SQLite 自动建表均已验证通过
- 当前阶段未做鉴权、工具调用、流式输出，符合任务边界

## 任务：B2-1 Review 反馈修复

### 做了什么
- 读取最新 `review/2026/03/23.md`，确认当前唯一阻塞问题是 Python 3.9 兼容性
- 将 `backend/models.py`、`backend/routers/chat.py`、`backend/services/llm.py` 中的 `X | None`、`list[dict]` 等较新类型写法改为 `Optional`、`List`、`Dict`
- 为 LLM 失败降级补充 `logging.warning`，避免真实模型联调失败时完全无日志
- 新增 `backend/.env.example`，补齐 `APP_DB_PATH`、`LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL`、`LLM_MOCK_MODE`、`LLM_TIMEOUT_SECONDS` 等配置模板
- 更新 `backend/README.md`，补充 `.env.example` 使用说明
- 重新完成 `uvicorn` 启动验证与 `/api/chat` 两次请求验证

### 改动文件
- `backend/models.py` - 修复 Python 3.9 不兼容的联合类型写法
- `backend/routers/chat.py` - 修复 Python 3.9 不兼容的联合类型写法
- `backend/services/llm.py` - 修复类型注解兼容性，并增加 LLM fallback 警告日志
- `backend/.env.example` - 新增本地环境变量模板文件
- `backend/README.md` - 补充 `.env.example` 使用提示
- `.project/.workflow/history-full/2026/03/23.md` - 追加 review 修复日志

### 关键决策
- 优先修复 review 中唯一阻塞项，保证服务至少能启动，再考虑非阻塞改进
- 兼容性修复采用 `typing.Optional/List/Dict`，不依赖额外安装 `eval_type_backport`
- 保留当前“两段数据库写入 + Mock 降级”实现不动，只处理阻塞问题和低成本改进，避免本轮引入新的行为回归

### 遇到的问题
- 本机没有 Python 3.9 解释器，无法直接在 3.9 环境做启动复验；本轮通过代码静态扫描确认已移除 `| None` 等不兼容写法，并在现有 Python 3.13 环境下重新完成启动与接口验证

### 当前结果
- Review 指出的阻塞项已修复
- `uvicorn` 可再次正常启动
- `/api/chat` 新建会话与追加消息验证再次通过，`sequence_no`、`updated_at`、`PRAGMA foreign_keys = ON` 行为正常
- LLM 未配置时 now 会输出 warning 日志后降级到 Mock，便于后续联调排障
