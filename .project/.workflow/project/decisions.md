# 重要决策记录

> 维护责任：`Leader Agent`
> 
> 说明：本文件只记录跨任务、跨阶段仍然有效的重要决策，不记录短期操作细节。

<!-- 记录跨任务生效的重要技术或产品决策 -->
<!-- 格式：决策内容 → 原因 + 日期 -->
<!-- 示例：暂不接入 LangChain → 当前阶段复杂度不必要，2026-03-17 -->

- 项目起步顺序调整为"数据库设计 → API 设计 → 后端实现 → 前端接入" → 先固定基础契约，减少后续返工，2026-03-17
- 对话先做非流式版本 → 降低前后端状态管理和工具调用整合复杂度，2026-03-17
- `question_tool` 的三级降级暂不进入第一阶段 → 先把核心聊天和基础工具链路跑通，2026-03-17
- 暂不引入 LangChain → 当前阶段优先选择更直接、更易解释的实现，2026-03-17
- 登录注册后置 → 当前优先保证演示主链路可用，2026-03-17
- 工作流实际路径为 `.project/.workflow/`（而非根目录 `.workflow/`）→ 因项目结构调整，所有 Agent 引用工作流文件时均以 `.project/.workflow/` 为准，2026-03-23
- SQLite 启动时必须显式执行 `PRAGMA foreign_keys = ON` → Review Agent 在 D-1 审查中提出，外键约束默认关闭会导致数据一致性风险，2026-03-23
- `knowledge_tool` 数据源从 Wikipedia API 切换为百度搜索 API（千帆 `web_search`）→ Wikipedia 在国内被墙，答辩演示不可控；百度搜索 API 国内稳定可用，每月 1500 次免费额度够用，中文教育内容覆盖好，2026-04-14
- `question_tool` 三级降级简化为两级（第一级本地题库 + 第三级 LLM 兜底），跳过第二级联网搜索出题 → 联网搜索结果格式不可控、解析复杂度高、答辩演示风险大；两级已能完整体现论文"多源异构数据"论点，2026-04-15
- 前端技术栈从 Vanilla JS 迁移到 React（Vite + React），时机为第 4 阶段 MCP 完成后、第 5 阶段用户体系之前 → 当前原生 JS 前端仅为功能验证用途；第 5 阶段起需批量新建页面（登录/注册/学习记录/推荐），统一用 React 开发效率更高；设计方案中的 Vue 3 对应调整为 React，2026-04-17
- MCP Client 连接复用优化暂不单独立项 → 当前每次新建 stdio 连接功能正确，性能问题在演示场景下不构成瓶颈；保留在 open-issues 中作为后续增强项，2026-04-17
- B4 学习分析 API 的 `weak_points` 修订为 `focus_areas`（学习热点） → 标签频次仅反映讨论频率不代表掌握薄弱，修正语义避免答辩被质疑分析逻辑，2026-04-20

## 使用规则

- 只有当某个决策会影响后续多个任务时，才写入本文件
- 短期任务限制优先写在 `current/context.md`
