# Review 2026-04-19

## B1 后端用户注册与登录接口复审

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] 当前默认开发密钥仍是 `dev-secret-key`，真实 JWT 解码时会触发 `InsecureKeyLengthWarning`；本轮不影响验收，因为 `JWT_SECRET` 已支持环境变量覆盖且任务明确允许开发默认值，但正式环境应改为 32 字节以上随机密钥。
- [ ] 历史 `demo_user` 仍是 `dev-only-hash` 测试账户，本轮兼容处理后会稳定返回 401 而非 500；这满足当前任务目标，但它不应被视为真实可登录账户。

## 是否满足 acceptance
- [x] 是
- [x] `backend/routers/auth.py` 存在，包含 `POST /api/register` 和 `POST /api/login`
- [x] `backend/main.py` 已注册 auth 路由
- [x] `backend/requirements.txt` 已新增 `bcrypt` 和 `PyJWT`
- [x] 真实 `POST /api/register` 发送 `{ username, password, grade, subject }` 返回 `201 + 用户信息`
- [x] 重复注册真实返回 `409 + USERNAME_EXISTS`
- [x] 真实 `POST /api/login` 发送 `{ username, password }` 返回 `200 + JWT + 用户信息`
- [x] 错误密码真实返回 `401`
- [x] JWT 含 `user_id`，已用 PyJWT 成功解码验证
- [x] 响应格式符合 `{ success, message, data } / { success, message, error_code }`
- [x] 后端服务可正常启动，现有 `/api/sessions` 和 `/api/chat` 真实烟测无回归
- [x] 密码未明文存储，定向测试和真实运行库查询都确认落库为 bcrypt 哈希

## open-issues 补充
- 默认开发密钥 `dev-secret-key` 长度低于 PyJWT 对 HS256 的推荐值，正式环境必须通过 `JWT_SECRET` 覆盖为更长随机密钥。

## 建议下一步
1. Leader 可收口 B1 register/login。
2. 后续进入 F1 前端登录注册页前，建议顺手把 `.env.example` 中的 `JWT_SECRET` 示例补成更长的开发密钥，避免继续触发安全告警。

---

## F1 前端登录注册页 + 路由配置复审

## 结论
- [ ] 未通过

## 阻塞问题
- [ ] `frontend/src/pages/AuthPage.jsx:79` - 登录和注册失败分支读取的是 `payload?.error?.message`，但后端 `auth.py` 的真实响应格式是顶层 `payload.message`。我已用真实 `POST http://127.0.0.1:5173/api/register` 和 `POST http://127.0.0.1:5173/api/login` 验证：重复注册返回 `{ success: false, message: "用户名已存在", error_code: "USERNAME_EXISTS" }`，错误密码返回 `{ success: false, message: "用户名或密码错误", error_code: "INVALID_CREDENTIALS" }`。按当前前端代码，这两条都会退回到通用文案，导致注册页不能准确显示“用户名已存在”，登录页也不能显示后端返回的具体错误提示，未满足 F1 的失败提示验收。

## 非阻塞问题
- [ ] `frontend/src/pages/AuthPage.jsx:27` - 认证页主题状态使用 `localStorage['theme']`，而对话页仍使用 `localStorage['frontend-theme']`；因此登录/注册页切换主题后，进入聊天页时主题不一定同步。这不影响本轮主链路，但会带来视觉状态不一致。
- [ ] 本轮 `npm test`、`npm run build`、`/login` `/register` `/chat` 路由烟测和 `/api/sessions` 代理烟测都通过，但浏览器层的登录跳转、登出跳转和视觉一致性仍建议在修复 blocker 后补一轮人工走查。

## 是否满足 acceptance
- [ ] 否
- [x] `package.json` 已包含 `react-router-dom`
- [x] 路由表已配置，包含 `/login`、`/register`、`/chat` 和 `/`
- [x] `npm test` 全绿，`npm run build` 通过
- [x] Vite 开发服务下 `GET /login`、`GET /register`、`GET /chat` 都可返回页面入口
- [x] `/api/register`、`/api/login` 通过前端代理可真实打到后端
- [x] `ProtectedRoute`、登录存 token、侧栏退出清 token 的代码路径已存在
- [x] 现有 `/api/sessions` 代理烟测仍返回 `200`，未见对话主链路被本轮立即破坏
- [ ] 但注册/登录失败提示当前不能准确映射后端返回消息，不能判定整轮通过

## open-issues 补充
- 若 F1 后续继续保留登录页独立主题切换，建议统一认证页与聊天页的主题存储 key，避免跨页面主题状态漂移。

## 建议下一步
1. 将 `AuthPage.jsx` 中失败提示改为读取 `payload?.message`，或统一兼容两种格式后再展示。
2. 修复后至少重跑一轮：重复用户名注册、错误密码登录、登录成功跳转 `/chat`、退出后跳回 `/login`。

---

## F1 前端登录注册页 + 路由配置复审（修复后）

## 结论
- [x] 通过

## 阻塞问题
- [ ] 无

## 非阻塞问题
- [ ] 当前认证流程的自动化覆盖仍以烟测为主，后续若继续迭代 F1，建议补 React Router / AuthPage 级测试，重点覆盖登录成功跳转和退出后回跳 `/login`。

## 是否满足 acceptance
- [x] 是
- [x] `package.json` 已包含 `react-router-dom`
- [x] 路由表仍包含 `/login`、`/register`、`/chat` 和 `/`
- [x] `AuthPage.jsx` 已改为读取 auth API 顶层 `payload.message`；真实前端代理验证：重复注册返回 `409 + 用户名已存在`，错误密码登录返回 `401 + 用户名或密码错误`
- [x] 认证页与聊天页已统一使用 `frontend-theme` 作为主题存储 key，跨页面主题状态不再分裂
- [x] 前端 `npm test`、`npm run build` 均通过
- [x] 后端鉴权与现有主链路回归 `13/13` 通过
- [x] 真实 `POST /api/register` 新用户注册成功，重复用户名返回 `USERNAME_EXISTS`
- [x] 真实 `POST /api/login` 正确密码返回 `200 + Bearer token`，错误密码返回 `401 + INVALID_CREDENTIALS`
- [x] 新注册用户已实际写入 `project-scipt/runtime/backend_runtime.db`，密码以 bcrypt 哈希落库，未明文存储
- [x] 真实 `/api/sessions`、`/api/chat` 烟测仍返回 `200`，未被本轮修复带坏
- [x] `ProtectedRoute`、登录存 token、侧栏退出清 token 并跳回 `/login` 的代码路径未被本轮破坏；结合你补充的“登录注册前端测试没有明显问题”，可判定 F1 主链路满足验收
- [x] 本轮未修改后端接口契约，满足“后端 API 不改动”的任务边界

## open-issues 补充
- 鉴权主链路已通过，但登录成功跳转、退出回跳和 token 过期处理目前仍主要依赖人工走查；若后续继续扩展用户系统，建议单独补一组组件/路由级回归测试。

## 建议下一步
1. Leader 可收口 F1 前端登录注册页 + 路由配置。
2. 如下一阶段马上接用户态会话隔离，建议优先把 JWT 鉴权真正接入 `/api/chat`、`/api/sessions`，替换当前的 `user_id=1` 临时实现。

---

## 项目级前后端联调复审（生产就绪视角）

## 结论
- [x] 当前迭代通过
- [ ] 项目整体生产就绪

## 阻塞问题
- [ ] 无（本轮 JWT 主链路与联调修复已闭环）

## 非阻塞问题
- [ ] `backend/routers/auth.py:107` - `/api/auth/me` 当前把“token 可解码但对应用户已不存在”的场景返回为 `404 USER_NOT_FOUND`。从前端现在把它当作登录态校验探针的用法看，返回 `401 + UNAUTHORIZED` 会更闭环；但考虑到项目仍处于未完成阶段、当前也没有正式的用户删除流程，这条更适合作为后续收口风险，而不是本轮 blocker。
- [ ] `frontend/src/components/Layout/AppLayout.jsx:21` - 挂载时虽然已经调用 `/api/auth/me` 校验 token，但成功响应被直接丢弃，没有顺手刷新本地 `edu_user`。首页和顶栏目前仍主要依赖 `localStorage['edu_user']` 展示用户名、年级和学科，因此本地缓存缺失、损坏或后端资料更新时，前端仍可能显示默认值或旧值。建议把 `/api/auth/me` 的 `200` 响应顺手同步回本地会话。
- [ ] `frontend/src/pages/HomePage/index.jsx:5` - Dashboard 当前仍以 `localStorage + 静态文案` 为主，未真正接入后端学习数据、会话数据或推荐数据。本轮不作为 blocker，但从“项目整体生产就绪”视角看，它仍属于未完成模块，而不是可交付的数据面板。

## 是否满足 acceptance
- [x] 是（针对本轮 JWT 接入、认证兜底与联调修复）
- [ ] 否（若按项目整体生产就绪口径）

## open-issues 补充
- 前后端 JWT 主链路、401 统一错误格式、认证页网络异常兜底、默认 `JWT_SECRET` 长度收紧均已落地；前端 `45/45` 测试、`npm run build`、后端 `29/29` 联调测试均通过。
- 当前更准确的判断是：本轮修复合理且可继续保留，但项目整体仍未完成，尤其 Dashboard 仍是静态原型页、受保护页面校验语义也还有边界风险，因此不宜把“项目整体生产就绪”作为当前收口结论。

## 建议下一步
1. Leader 如按当前迭代收口，可将这轮判断为“通过，但项目整体未完成”，不要把未完成模块与本轮修复混为一谈。
2. 后续若继续完善认证闭环，优先处理 `/api/auth/me` 的孤儿用户分支，并顺手把成功返回的用户资料回写到本地 auth session。
3. Dashboard 若要进入正式联调阶段，需要单独立项接真实后端数据；若暂不做，请明确它仍是静态原型页。
