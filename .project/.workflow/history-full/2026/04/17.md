# 2026-04-17

## 任务：M4-2 将 knowledge_tool 和 question_tool 包装为 MCP 工具并切换调用路径

### 做了什么
- 先按 TDD 补红灯测试，覆盖 `backend/mcp_server.py` 是否注册 3 个 MCP 工具，以及 `backend/services/llm.py` 中 knowledge/question 两条链路是否改为 MCP 优先、失败回退 direct call。
- 扩展 `backend/mcp_server.py`，把 `knowledge_tool` 和 `question_tool` 注册进现有 `FastMCP("EduTools")`，保持脚本模式与模块模式入口兼容。
- 修改 `backend/services/llm.py`，将 `_build_knowledge_reply`、`_build_question_reply` 改为异步 MCP 优先调用，并新增 `knowledge_tool`、`question_tool` 的 direct fallback。
- 调整 LLM tool-call loop，让 OpenAI-compatible `tool_calls` 执行 `knowledge_tool`、`question_tool` 时也统一走 `call_mcp_tool(...)`，而不是继续直调本地函数。
- 重跑后端与前端回归，并做真实 MCP `list_tools()` 和真实 `/api/chat` 三条工具链联调验证。

### 改动文件
- `backend/mcp_server.py` - 新增 `knowledge_tool`、`question_tool` 的 MCP 注册。
- `backend/services/llm.py` - knowledge/question 改为 MCP 优先、失败回退 direct call，并同步 tool-call loop。
- `backend/tests/test_mcp_server.py` - 新增 3 个 MCP 工具注册断言。
- `backend/tests/test_tool_triggering.py` - 新增 knowledge/question 走 MCP 与 fallback 的触发回归。
- `backend/tests/test_llm_tool_loop.py` - 调整 knowledge/question 的 tool-call loop 断言，改为校验 `call_mcp_tool(...)`。
- `backend/tests/test_chat_tools_api.py` - `/api/chat` 集成测试改为校验 knowledge/question 经由 MCP。

### 关键决策
- 按 `task.md` 边界执行，只迁移 `knowledge_tool` 和 `question_tool` 到 MCP，不提前做连接复用、性能优化或前端改造。
- 保持 `call_mcp_tool(...)` 失败即回退 direct function，避免 Windows 沙箱里的 `stdio` 权限问题影响服务可用性。
- 保留现有 `mcp_client.py` 与 `TOOL_DEFINITIONS` 契约不动，把改动面收敛在 MCP server 注册和 `llm.py` 调用路径。

### 遇到的问题
- 旧的 history 文件已经存在编码问题，终端查看会显示乱码；本轮仅增量补记，不扩大历史文件修复范围。
- Windows 默认沙箱下真实 MCP `stdio` 子进程仍可能触发 `PermissionError: [WinError 5]`，因此真实 MCP `list_tools()` 证据继续在提权环境获取。

### 当前结果
- 真实 MCP `list_tools()` 已返回 `['calculator_tool', 'knowledge_tool', 'question_tool']`。
- 真实 `/api/chat` 已验证：
- `什么是勾股定理` -> `knowledge_tool`
- `给我出一道关于二次函数的题` -> `question_tool`
- `计算 2 的 10 次方` -> `calculator_tool`，结果 `1024`
- 后端回归 `34/34` 通过，前端 `npm test` 21 条测试全绿。

## 任务：B3-3 question_tool 最小可用版与 C-Eval 数据准备

### 做了什么
- 新增 `backend/tools/download_ceval.py`，从 HuggingFace 按学科下载 C-Eval parquet，过滤无答案数据并导出为 CSV。
- 新增 `backend/tools/import_ceval.py`，将 `backend/tools/ceval_data/` 下的 CSV 导入 SQLite `question_bank` 表，并抽取基础知识点标签。
- 扩展 `backend/database.py`，创建 `question_bank` 表及索引。
- 新增 `backend/tools/question_tool.py`，实现本地题库命中优先、LLM/mock 兜底的出题工具。
- 在 `backend/services/llm.py` 中接入 `question_tool` 的 tool definition、mock 触发和 tool call 执行链路。
- 在 `backend/tests/` 下补充下载脚本、导入脚本、question_tool、LLM tool loop、/api/chat 集成与触发回归测试。
- 更新前端 `frontend/src/chat-view.js` 和 `frontend/tests/tool-status.test.js`，补齐 `question_tool -> 出题` 的标签映射与回归测试。
- 安装 `pandas`、`pyarrow` 依赖，真实下载 7 个学科 C-Eval 数据并导入运行时数据库。
- 根据 Review 阻塞项修复 `backend/tools/question_tool.py`、`backend/tools/import_ceval.py` 的 SQLite 连接泄漏，并同步修正相关测试查询写法。
- 调整 `backend/services/llm.py` 的 live 路由策略：真实 LLM 模式下也先走本地工具判定和弱触发 suggestion，只有普通闲聊才继续交给外部 LLM。
- 为 live 本地优先策略补充 `backend/tests/test_tool_triggering.py`、`backend/tests/test_llm_tool_loop.py` 回归测试，并验证全量后端测试通过。
- 将 `backend/tools/knowledge_tool.py` 切换到百度千帆“智能搜索生成”新接口 `/v2/ai_search/chat/completions`，优先解析生成摘要，引用结果做兜底展示；补充对应单测与环境模板。

### 改动文件
- `backend/requirements.txt` - 新增 `pandas`、`pyarrow` 依赖。
- `backend/tools/download_ceval.py` - 新增 C-Eval 下载与 parquet 转 CSV 脚本。
- `backend/tools/import_ceval.py` - 新增 CSV 导入 `question_bank` 的脚本。
- `backend/tools/question_tool.py` - 新增出题工具实现。
- `backend/tools/__init__.py` - 导出 `question_tool`。
- `backend/database.py` - 新增 `question_bank` 表与索引。
- `backend/services/llm.py` - 接入 `question_tool` 的识别、执行和回复格式化逻辑。
- `backend/tests/test_download_ceval.py` - 新增下载脚本测试。
- `backend/tests/test_question_tool.py` - 新增 question_tool 与导入脚本测试。
- `backend/tests/test_llm_tool_loop.py` - 新增 question_tool tool-call 回归测试。
- `backend/tests/test_chat_tools_api.py` - 新增 `/api/chat` question_tool 集成测试。
- `backend/tests/test_tool_triggering.py` - 新增 mock 模式 question_tool 触发测试。
- `frontend/src/chat-view.js` - 补齐 question_tool 标签映射。
- `frontend/tests/tool-status.test.js` - 新增前端工具标签回归覆盖。
- `backend/tools/question_tool.py` - 将本地题库查询改为显式关闭连接。
- `backend/tools/import_ceval.py` - 将导入逻辑改为显式关闭连接。
- `backend/tests/test_question_tool.py` - 调整测试查询，避免测试代码本身留下未关闭连接。
- `backend/services/llm.py` - 将本地工具优先判定前移到 live 模式。
- `backend/tests/test_tool_triggering.py` - 新增 live 模式下知识问句优先走工具、普通闲聊继续走 LLM 的回归。
- `backend/tests/test_llm_tool_loop.py` - 调整 tool-call loop 用例，避免与新的本地优先规则冲突。
- `backend/tools/knowledge_tool.py` - 切换到百度千帆新搜索生成接口并兼容 `choices + references` 返回结构。
- `backend/tests/test_knowledge_tool.py` - 更新为新接口的请求体和解析断言。
- `backend/.env.example` - 增加可选的 `BAIDU_SEARCH_MODEL` 配置项。

### 关键决策
- 按 roadmap/设计方案与最新决策，`question_tool` 只实现“本地题库 + LLM 兜底”两级策略，不接联网搜索出题。
- C-Eval 下载不使用 `datasets`，改为 `httpx + pandas + pyarrow`，避开之前 HuggingFace API 兼容问题。
- 真实验证使用 `project-scipt/runtime/backend_runtime.db`，避免沿用 `backend/app.db` 的只读/沙箱问题。
- 对本地题库知识点抽取只保留基础关键词映射，满足最小命中要求，不在本轮扩成复杂标签系统。

### 遇到的问题
- 沙箱内执行依赖安装和 SQLite 导入时出现网络/磁盘 I/O 限制，需要提权后完成真实安装与导入。
- PowerShell 直接内联中文 prompt 验证时会被编码成 `?`，导致 mock 触发判断失真；改用 Unicode 转义后完成可靠验证。
- 前端 `question_tool` 标签最初落在错误常量中，虽不影响渲染，但已整理回 `TOOL_LABELS`，避免 Review 卡细节。
- SQLite `Connection` 的上下文管理器只处理事务不自动关闭连接，Review 指出的 `ResourceWarning` 需要通过显式 `close()` 修复。

### 当前结果
- `backend/tools/ceval_data/` 已生成 7 个学科 CSV，真实导入运行时数据库共 1396 道题。
- `question_tool` 已能在 `二次函数`、`光合作用`、`牛顿第二定律` 等主题上命中本地 C-Eval 题库；`勾股定理` 等未覆盖主题会回落到 `llm_generated`。
- `/api/chat` 已接入 `question_tool`，`tools_used` 中可见 `tool_name == "question_tool"`。
- 前后端回归测试均通过，B3-3 已达到当前 acceptance 的最小可用范围。
- Review 指向的后端全量回归已复跑通过，输出中不再出现 `ResourceWarning: unclosed database`。
- 真实 LLM 模式现在也会对明显的计算题、知识问句和出题请求优先走本地工具，不再把这类请求完全交给外部模型自行决定。
- 新版 `knowledge_tool` 已用真实百度 key 验证：不传 `BAIDU_SEARCH_MODEL` 时，新接口可稳定返回搜索结果；传 `ernie-4.5-turbo-32k` 时当前账号下会超时，因此默认保持不传 model。

## 任务：M4-1 建立最小 MCP Server 骨架并将 calculator_tool 包装为 MCP 工具

### 做了什么
- 新增 `backend/mcp_server.py`，使用 `FastMCP("EduTools")` 注册 `calculator_tool`，并保持 `python -m backend.mcp_server` 可运行。
- 新增 `backend/services/mcp_client.py`，通过 `ClientSession + stdio_client` 启动本地 MCP 子进程、调用工具并提取文本结果。
- 修改 `backend/services/llm.py`，把 `calculator_tool` 的直接调用替换为“优先走 MCP，失败时 fallback 到原本 direct function”的路径，覆盖本地直达回复和 LLM tool-call loop 两条链路。
- 更新 `backend/requirements.txt`，追加 `mcp>=1.0.0` 并安装本地 SDK，实际安装版本为 `mcp 1.27.0`。
- 先按 TDD 修改 `backend/tests/test_tool_triggering.py`、`backend/tests/test_llm_tool_loop.py`、`backend/tests/test_chat_tools_api.py`，补齐 calculator 经由 MCP 与 fallback 的回归断言。
- 在提权环境里真实验证 MCP server 可列出 `calculator_tool`，并成功完成 `call_mcp_tool('calculator_tool', {'expression': '2^10'}) -> 1024`。
- 启动真实后端后验证 `/api/chat`：计算题走 `calculator_tool`，`knowledge_tool` 和 `question_tool` 仍正常工作。

### 改动文件
- `backend/mcp_server.py` - 新增最小 MCP Server，注册 `calculator_tool`。
- `backend/services/mcp_client.py` - 新增 stdio MCP client 封装与结果提取逻辑。
- `backend/services/llm.py` - calculator 路径切换为 MCP 优先、失败降级 direct call。
- `backend/requirements.txt` - 增加 `mcp>=1.0.0`。
- `backend/tests/test_tool_triggering.py` - 补 direct calculator 走 MCP 与 fallback 回归。
- `backend/tests/test_llm_tool_loop.py` - 补 calculator tool-call loop 走 MCP 断言。
- `backend/tests/test_chat_tools_api.py` - 补 `/api/chat` calculator 经由 MCP 的 API 回归。

### 关键决策
- 本轮严格按 `M4-1` 边界执行，只迁 `calculator_tool` 到 MCP；`knowledge_tool` 和 `question_tool` 保持 direct call，留给 `M4-2`。
- Client 连接生命周期先采用“每次调用新建 stdio 连接”的最小实现，优先跑通链路；持久连接优化不在本轮展开。
- Windows 沙箱内真实 stdio 子进程会被 `PermissionError: [WinError 5]` 拦住，因此真实 MCP 调用证据改用提权环境获取；业务代码仍保留 direct fallback，保证服务可用。

### 遇到的问题
- 本地环境原先未安装 `mcp`，需要先更新依赖并根据实际安装的 `mcp 1.27.0` 包结构确认 `FastMCP`、`ClientSession`、`StdioServerParameters` 的真实 API。
- Windows 沙箱中 `stdio_client` 创建子进程时会命中 `PermissionError: [WinError 5] 拒绝访问`，导致真实 MCP 调用无法在默认沙箱里完成。
- API 类 SQLite 测试在默认沙箱里仍会触发 `disk I/O error`，因此后端整组回归继续沿用可写环境执行。

### 当前结果
- `backend/mcp_server.py` 已存在，并能在提权环境下被真实 stdio client 启动。
- 真实 `list_tools()` 返回 `['calculator_tool']`，说明工具注册成功。
- 真实 `call_mcp_tool('calculator_tool', {'expression': '2^10'})` 返回 `1024`。
- 后端回归 `python -m unittest backend.tests.test_sessions_api backend.tests.test_calculator_tool backend.tests.test_knowledge_tool backend.tests.test_llm_tool_loop backend.tests.test_chat_tools_api backend.tests.test_tool_triggering` 共 30/30 通过。
- 前端回归 `npm test` 全部通过，无前端回归。
- 真实 `/api/chat` 已验证：
  - `计算 2 的 10 次方` → `tools_used[0].tool_name == "calculator_tool"`，结果 `1024`
  - `什么是勾股定理` → `knowledge_tool` 正常返回
  - `给我出一道关于二次函数的题` → `question_tool` 正常返回本地题库题目

## 任务：M4-1 review 修复 — mcp_server 脚本模式启动

### 做了什么
- 根据 review 复现 `python backend/mcp_server.py` 的脚本模式失败，确认根因是脚本模式下 `sys.path` 中缺少项目根目录，导致 `from backend.tools...` 直接报 `ModuleNotFoundError`。
- 新增 `backend/tests/test_mcp_server.py`，用子进程模拟脚本模式运行，并在测试里将 `FastMCP.run` 打桩为 no-op，确保回归测试能锁住这个入口问题。
- 修复 `backend/mcp_server.py`，仅在脚本模式下把项目根目录补入 `sys.path`，保证 `python backend/mcp_server.py` 与 `python -m backend.mcp_server` 两种启动方式都可用。

### 改动文件
- `backend/tests/test_mcp_server.py` - 新增脚本模式启动回归测试。
- `backend/mcp_server.py` - 增加脚本模式下的项目根目录引导逻辑。

### 关键决策
- 修复范围只针对 review 指向的启动入口兼容，不改动 MCP server 注册逻辑和 client 调用逻辑。
- 采用“仅脚本模式补路径”的收口方式，避免影响模块模式和正常 import 路径。

### 遇到的问题
- 默认沙箱下后端 SQLite 测试目录仍会触发 `PermissionError` / `disk I/O error`，所以后端整组回归改在提权环境复验。
- 直接前台运行 `python backend/mcp_server.py` 会因 stdio stdin 关闭而快速退出；本轮验证重点是“不再因 import 路径直接崩溃”，并结合真实 `call_mcp_tool()` 补充 MCP 链路证据。

### 当前结果
- `backend.tests.test_mcp_server` 已从失败变为通过，锁住脚本模式启动兼容。
- 提权环境下后端回归 `python -m unittest backend.tests.test_mcp_server backend.tests.test_sessions_api backend.tests.test_calculator_tool backend.tests.test_knowledge_tool backend.tests.test_llm_tool_loop backend.tests.test_chat_tools_api backend.tests.test_tool_triggering` 共 31/31 通过。
- 前端回归 `npm test` 再次全绿，无旁路回归。
- 真实 `call_mcp_tool('calculator_tool', {'expression': '2^10'})` 再次返回 `1024`。
- 真实 `/api/chat` 复验 `计算 2 的 10 次方`，`tools_used[0].tool_name == "calculator_tool"` 且结果为 `1024`。
- `python backend/mcp_server.py` 与 `python -m backend.mcp_server` 均已不再出现 `ModuleNotFoundError`。
