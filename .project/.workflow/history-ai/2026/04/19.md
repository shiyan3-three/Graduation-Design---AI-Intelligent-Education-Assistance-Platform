# 2026-04-19

## 当日关键进展

- 完成 B1：实现后端 `POST /api/register` 与 `POST /api/login`，使用 bcrypt + PyJWT。
- 完成 F1：引入 `react-router-dom` 重构前端路由（`/login`, `/register`, `/chat`, `/`），实现双面表单登录/注册页、`ProtectedRoute` 路由守卫。
- 完成 FH-1：构建带 `GlobalHeader` 的全局 `AppLayout`，落地首? Dashboard 仪表盘页。
- 完成多用户数据隔离与后端强制鉴权：新增 `get_current_user_id` 依赖解析 JWT，替换 `chat.py` 和 `sessions.py` 中的 `user_id = 1` 硬编码。
- 完善鉴权与安全性：全局统一处理 401 错误响应格式；新增 `GET /api/auth/me` 供前端异步验证 token；收紧 `JWT_SECRET` 默认长度以消除安全警告。
- 前后端全套测试覆盖增强：API 集成测试 29/29 通过，前端单元测试 45/45 通过。

## 关键决策

- 前端统一主题状态树，清除各页面的独立副本，仅由 `AppLayout` 通过 `useGlobalTheme` 维护单源主题状态向下传递。
- 后端使用 FastAPI 接口级依赖注入拦截用户签权，未采用全局 Middleware，保持权限细粒度可视。
- 前端 `requestJson()` 集中注入 `Authorization` Header 并统一拦截 401 自动执行 `clearAuthSession` (剔除等同 `/api/login` 的公开路径)。
- `AppLayout` 增加 `fail-open` 策略：验证 token 遇到网络未连接报错时，不武断登出离线合法用户。

## 当前遗留问题

- B1 后端用户注册与登录接口、F1 均已彻底完成，同时清空了对应的相关任务债务。第 5 阶段正式完结。

## 给下一个 Agent 的交接

- 第 5 阶段（用户体系 B1 + F1）已清空完结。目前项目处于第 5.5 阶段（FH-1 仪表盘完成）。
- 接下来可开始第 6 阶段学习记录（B4 后端接口开发 + F3 前端页）。
- 前端 `api.js` 中新增了 `saveAuthSession(token, userProfile)` 与 `clearAuthSession()` 需要在业务逻辑复用。
- 后端所有原有业务路由目前均已强制校验 `Authorization: Bearer <token>`，开发调用需携带合法 JWT。
